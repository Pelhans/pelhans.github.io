---
title: "LLaMA2"
date: 2026-08-08
layout: post
liquid: false
tags: [语雀, 计算机, 大模型, 1._模型调研, 1.3_大模型逐个学习笔记]
categories: [计算机]
yuque: true
---

{% raw %}
先来经典的 LLaMA2 吧，然后再横向看一下从 LLaMA 到 LLaMA3 的的进化细节。

# LLaMA2

## 代码

> git clone [https://github.com/facebookresearch/llama.git](https://github.com/facebookresearch/llama.git)

单看推理的话，就是先调用 build 加载模型，然后调用 generate 进行生成。分开看。

### build

[模型结构概览](https://zhuanlan.zhihu.com/p/677125915)

![image.png](/img/yuque/计算机/大模型/1._模型调研/1.3_大模型逐个学习笔记/assets/LLaMA_家族-1.png)

参数：

-   ckpt\_dir
-   tokenizer\_path：

-   用的什么 tokenizer？

-   max\_seq\_len：输入文本的最大长度
-   max\_batch\_size：推理的最大 batch\_size。太大会怎样？
-   model\_parallel\_size：并行模型的 GPU 数量
-   seed

流程：

-   initialize\_model\_parallel：初始化并行模型所需要的环境：

-   world\_size通常指的是分布式训练环境中的进程总数。每个进程可以包含多张卡。

-   加载模型 ：

-   model = Transformer(model\_args)
-   model.load\_state\_dict(checkpoint, strict=False)
-   tokenizer = Tokenizer(model\_path=tokenizer\_path)
-   Llama(model, tokenizer)

#### Transformer

1.  token embedding

```python
self.tok_embeddings = ParallelEmbedding(
            params.vocab_size, params.dim, init_method=lambda x: x
        )
```

FairScale 是一个用于高性能和大规模训练的 PyTorch 扩展库。该库扩展了基本的 PyTorch 功能，同时添加了新的 SOTA 扩展技术。FairScale 以可组合模块和易于使用的 API 的形式提供最新的分布式训练技术。

1.  ParallelEmbedding：

1.  默认 xavier\_normal\_ 初始化
2.  不考虑并行的话，就是 xavier\_normal\_(Parameter(torch.Tensor(self.num\_embeddings, self.embedding\_dim\_per\_partition)))

1.  在PyTorch中，Parameter是一个特殊的张量（Tensor），它用于表示模型中的参数，比如神经网络中的权重和偏置。Parameter对象与常规的Tensor对象有以下不同之处：

1.  **自动梯度**：Parameter对象会自动注册为计算图中的一部分，这意味着在使用这些参数进行计算时，PyTorch会自动追踪梯度。这对于在反向传播中更新模型参数至关重要。
2.  **优化器集成**：Parameter对象可以直接传递给优化器（如torch.optim.Adam或torch.optim.SGD），优化器会使用这些参数来执行梯度下降等优化算法。
3.  **模型状态字典**：当保存或加载模型状态时，Parameter对象会被包含在模型的状态字典中。
4.  **参数化**：Parameter对象通常用于定义模型的可训练参数，它们在模型初始化时被创建，并在训练过程中被更新。

注册 TransformerBlock

```python
self.layers = torch.nn.ModuleList()
        for layer_id in range(params.n_layers):
            self.layers.append(TransformerBlock(layer_id, params))
```

1.  ModuleList

1.  ModuleList主要用于包装一些模块，使它们可以像单个模块一样被注册和使用。当你有一个模型需要包含多个相同类型的子模块时，ModuleList是非常有用的。
2.  **支持索引**：可以通过索引来访问ModuleList中的单个模块。

2.  TransformerBlock

1.  参数：

1.  layerid
2.  n\_heads
3.  dim
4.  head\_dim = dim//n\_heads

2.  attention

1.  初始化

1.  ColumnParallelLinear：

1.  **self.wq = ColumnParallelLinear( args.dim, args.n\_heads \* self.head\_dim, bias=False, gather\_output=False, init\_method=lambda x: x, )**
2.  **与****nn.Linear****接口兼容**：ColumnParallelLinear的接口与标准的nn.Linear模块类似，使得从标准线性层迁移到列并行线性层变得容易。
3.  它实现了一种特殊的线性层，用于在分布式设置中进行模型并行，将输入特征的列分配到不同的GPU上。每个GPU计算输入的一部分列与权重矩阵的一部分行的乘积。
4.  The linear layer is defined as Y = XA + b. A 。这里没加 bias，所以就是一个并行版的 W 权重矩阵，第一个参数是 input维度, 第二个参数是输出维度
5.  默认也是 xavier\_normal\_ 初始化
6.  共初始化了 wq（dim，n\_heads \* self.head\_dim）、wk（dim，n\_kv\_heads \* self.head\_dim）、wv（dim，n\_kv\_heads \* self.head\_dim）、wo（n\_heads \* self.head\_dim，dim） 四个

2.  还有两个 cache\_k 和 cache\_v：

1.  cache\_k = torch.zeros((args.max\_batch\_size,args.max\_seq\_len,self.n\_local\_kv\_heads,self.head\_dim,)).cuda()
2.  cache\_v = torch.zeros((args.max\_batch\_size,args.max\_seq\_len,self.n\_local\_kv\_heads,self.head\_dim,)).cuda()

2.  forward 前向传播：

1.  分别乘上权重进行映射：

1.  xq, xk, xv = self.wq(x), self.wk(x), self.wv(x)

2.  分别 reshape 一下

1.  xq = xq.view(bsz, seqlen, self.n\_local\_heads, self.head\_dim)
2.  xk = xk.view(bsz, seqlen, self.n\_local\_kv\_heads, self.head\_dim)
3.  xv = xv.view(bsz, seqlen, self.n\_local\_kv\_heads, self.head\_dim)

3.  加上 position embedding ：

1.  xq, xk = apply\_rotary\_emb(xq, xk, freqs\_cis=freqs\_cis)
2.  rope 的计算：

4.  将self.cache\_k张量移动到与xq张量相同的设备上，方便后续处理

1.  self.cache\_k = self.cache\_k.to(xq)

5.  计算正常的 attention

3.  FeedForward：self.w2(F.silu(self.w1(x)) \* self.w3(x))

1.  silu：x\*sigmoid(x)，大于0部分像 reLU，小于0部分非0，避免死区

4.  RMSNorm：见 [https://www.yuque.com/yuqueyonghuyit3fz/ni7q4d/fb14uufg25og3zp4#dsfCF](https://www.yuque.com/yuqueyonghuyit3fz/ni7q4d/fb14uufg25og3zp4#dsfCF)

1.  除以均方根，只缩放，不均值归零

5.  forward：

1.  计算 attention：h = x + self.attention(self.attention\_norm(x), start\_pos, freqs\_cis, mask )
2.  加 ffn和 norm（先 norm，后 ffn）：out = h + self.feed\_forward(self.ffn\_norm(h))

3.  Transformer 的 forward：

1.  输入是文字 tokens (\_bsz, seq\_len)
2.  token 向量 embedding
3.  生成 mask
4.  逐层运行
5.  对输出进行 norm
6.  FFN 输出，得到词表每个token的分数

#### 加载 ckpt 文件 load\_state\_dict

## generate 生成

1.  第一轮，生成第一个token 时

1.  调用模型走 Transformer（此时内部 attention 的q、k、v 都是完整的 prompt 输入），输出词表中各个未知的 logits
2.  调用 softmax 得到概率分布，计算 softmax 的时候考虑了 tempature，
3.  使用 top-p 采样，得到本次输出 token
4.  把新的输出放到输入 tokens 里

2.  第二轮，生成第二个 token 时

1.  此时前向传播给模型的是新生成的那个 token logits = self.model.forward(tokens[:, prev\_pos:cur\_pos], prev\_pos)
2.  此时的 seqlen 是1，对它进行编码和计算 wq、wk、wv，并加上位置向量
3.  把新生成的 token 放到缓存里面 self.cache\_k[:bsz, start\_pos : start\_pos + seqlen] = xk
4.  从缓存里取出从开始到新生成 token 的 key 和 value
5.  后面就正常了，然后到下一层 att 也是一样，输入是一个 token 的编码。

## text\_completion

1.  先调用 tokenizer 进行编码

1.  prompt\_tokens = [self.tokenizer.encode(x, bos=True, eos=False) for x in prompts]

2.  调用上面的 generate 函数进行生成

## chat\_completion

在 text\_completion 的基础上，加上一些 role、system 相关信息。

# LLAMA2 的参数量估计

首先明确 7B 的是 4096维。词表大小 32k，batch 4M，数据量 2T。层数 32，是 MHA，头数 32，头维度 128

llama2-7B参数量计算如下：

-   **输入embedding**：4096\*32000=131072000，其中4096为上下文长度，32000为vocab\_size
-   **TransformerBlock**：32\*(67108864+135266304+8192)=6476267520

-   Attention(wq、wk、wv、wo)：4096\*4096=16777216，4\*16777216=67108864
-   FeedForward(w1、w2、w3)：4096\*(2\*4\*4096//3)=450887768，3\*45088768=135266304
-   attention\_norm+ffn\_norm：4096+4096=8192

-   **外层归一化**：4096
-   **输出映射**：4096\*32000=131072000
-   **总参数量**：131072000+6476267520+4096+131072000=**6738415616**

# Ghost attention

假设你已经有了一个多轮对话数据集，如（用户消息1，回复1；用户消息2，回复2）；  
1、指定一个希望多轮对话整体应该遵循的指令(可以称为system prompt，如人设)；  
2、将这条指令添加到普通多轮对话的每个用户消息中（舍去之前的回复）；  
3、使用最新一轮的RLHF模型对多轮中每个用户的消息生成多次的结果（此过程为采样），并使用RM选择最好的结果（llama2中提出的拒绝采样的步骤），这样就得到了符合指令的多轮对话数据。  
4、针对合成的数据，删除除了第一轮用户消息以外的所有添加的指令；然后去sft训练模型。  
5、训练过程中：将多轮数据中前面几轮的所有loss设置为0，不参与训练。（专门训多轮以后的system指令遵从能力）

这样：模型将被训练认为最前面的system指令是对话中非常重要的部分，即使是在多轮以后，也应该具有保持system指令的能力。

# GQA（Grouped-query attention）

多个 head 内共享 key 和 value。如下图所示：

![image.png](/img/yuque/计算机/大模型/1._模型调研/1.3_大模型逐个学习笔记/assets/LLaMA_家族-2.png)

可以显著减少内存占用。

还有更激进的方案 MQA，即所有头公用一个 key 和 value。

要想在已有的模型里用 GQA，可以选取一些头取 mean 来初始化继续训一下。

# 大模型的解码

如何从这个概率分布中选择下一个单词呢？以下是几种常用的方法：

-   **贪心解码**（Greedy Decoding）：直接选择概率最高的单词。这种方法简单高效，但是可能会导致生成的文本过于单调和重复。
-   **随机采样**（Random Sampling）：按照概率分布随机选择一个单词。这种方法可以增加生成的多样性，但是可能会导致生成的文本不连贯和无意义。
-   **Beam Search**：维护一个大小为 k 的候选序列集合，每一步从每个候选序列的概率分布中选择概率最高的 k 个单词，然后保留总概率最高的 k 个候选序列。这种方法可以平衡生成的质量和多样性，但是可能会导致生成的文本过于保守和不自然。

以上方法都有各自的问题，而 **top-k** 采样和 **top-p** 采样是介于贪心解码和随机采样之间的方法，也是目前大模型解码策略中常用的方法。

## top-k

贪心解码是一种合理的策略，但也有一些缺点。例如，输出可能会陷入重复循环。想想智能手机自动建议中的建议。当你不断地选择建议最高的单词时，它可能会变成重复的句子。

Top-k 采样是对前面“贪心策略”的优化，它从排名前 k 的 token 中进行抽样，允许其他分数或概率较高的token 也有机会被选中。在很多情况下，这种抽样带来的随机性有助于提高生成质量。

top-k 采样的思路是，在每一步，只从概率最高的 k 个单词中进行随机采样，而不考虑其他低概率的单词。例如，如果 k=2，那么我们只从女孩、鞋子中选择一个单词，而不考虑大象、西瓜等其他单词。这样可以避免采样到一些不合适或不相关的单词，同时也可以保留一些有趣或有创意的单词。

通过调整 k 的大小，即可控制采样列表的大小。“贪心策略”其实就是 k = 1的 top-k 采样。

总结一下，top-k 有以下有点：

-   它可以根据不同的输入文本动态调整候选单词的数量，而不是固定为 k 个。这是因为不同的输入文本可能会导致不同的概率分布，有些分布可能比较平坦，有些分布可能比较尖锐。如果分布比较平坦，那么前 k 个单词可能都有相近的概率，那么我们就可以从中进行随机采样；如果分布比较尖锐，那么前 k 个单词可能会占据绝大部分概率，那么我们就可以近似地进行贪心解码。
-   它可以通过调整 k 的大小来控制生成的多样性和质量。一般来说，k 越大，生成的多样性越高，但是生成的质量越低；k 越小，生成的质量越高，但是生成的多样性越低。因此，我们可以根据不同的任务和场景来选择合适的k 值。
-   它可以与其他解码策略结合使用，例如温度调节（Temperature Scaling）、重复惩罚（Repetition Penalty）、长度惩罚（Length Penalty）等，来进一步优化生成的效果。

但是 top-k 也有一些缺点，比如：

-   它可能会导致生成的文本不符合常识或逻辑。这是因为 top-k 采样只考虑了单词的概率，而没有考虑单词之间的语义和语法关系。例如，如果输入文本是“我喜欢吃”，那么即使饺子的概率最高，也不一定是最合适的选择，因为可能用户更喜欢吃其他食物。
-   它可能会导致生成的文本过于简单或无聊。这是因为 top-k 采样只考虑了概率最高的 k 个单词，而没有考虑其他低概率但有意义或有创意的单词。例如，如果输入文本是“我喜欢吃”，那么即使苹果、饺子和火锅都是合理的选择，也不一定是最有趣或最惊喜的选择，因为可能用户更喜欢吃一些特别或新奇的食物。

因此，我们通常会考虑 top-k 和其它策略结合，比如 top-p。

## top-p

op-k 有一个缺陷，那就是“k 值取多少是最优的？”非常难确定。于是出现了动态设置 token 候选列表大小策略——即核采样（Nucleus Sampling）。

top-p 采样的思路是，在每一步，只从累积概率超过某个阈值 p 的最小单词集合中进行随机采样，而不考虑其他低概率的单词。这种方法也被称为**核采样**（nucleus sampling），因为它只关注概率分布的核心部分，而忽略了尾部部分。例如，如果 p=0.9，那么我们只从累积概率达到 0.9 的最小单词集合中选择一个单词，而不考虑其他累积概率小于 0.9 的单词。这样可以避免采样到一些不合适或不相关的单词，同时也可以保留一些有趣或有创意的单词。

# Normallize

[https://www.yuque.com/yuqueyonghuyit3fz/ni7q4d/fb14uufg25og3zp4#dsfCF](https://www.yuque.com/yuqueyonghuyit3fz/ni7q4d/fb14uufg25og3zp4#dsfCF)

# KV cache

代码上看还是比较直观的，就是先把 cache\_k 挪到和 xq 相同的 device 上。然后将 k 和 v 的值存储到attention 内部，后面计算的时候直接取就可以了，这样避免了重复计算

```python
// 移动到相同设备上
self.cache_k = self.cache_k.to(xq)
self.cache_v = self.cache_v.to(xq)

// 将当前 k、v 值缓存下来
self.cache_k[:bsz, start_pos : start_pos + seqlen] = xk
self.cache_v[:bsz, start_pos : start_pos + seqlen] = xv

// 取出从开始到现在位置的缓存值
keys = self.cache_k[:bsz, : start_pos + seqlen]
values = self.cache_v[:bsz, : start_pos + seqlen]
```

问题：

1.  start\_pos 看入参说明是缓存开始的位置，那 xq 不是完整的？看着不像啊。

# Positon Embedding

## 旋转位置编码 RoPE（Rotary Position Embedding）

### 原理

旋转位置编码里的旋转就是给输入乘上一个旋转矩阵，使其旋转 (m-n) \* theta 角，用来表达两个输入向量位置 (m, n) 间的相对位置信息。下面介绍原理。

假设有输入 词向量 $x_m$ 和 $x_n$。分别经过 self-attention 的 q、k 映射得到 $q_m = W_{q}x_{m}$、$k_n = W_{k}x_{n}$。

现在需要加上位置向量信息，加上位置信息后的输出用 $f_q(q_{m}, m)$ 和 $f_k(q_{n}, n)$ 表示。那 $f_q$ 的具体形式是什么？它要满足两个条件：

1.  与输入 $q_m$是乘法组合的（比加法更自然）
2.  能充分表达相对位置信息， 即两个向量的内积$<f_q(x_{m}, m),f_k(x_{n}, n)>$表达式里能体现 m, n 间的位置关系。

一个自然的想法是乘以 $e^{im\theta}$感觉的旋转矩阵，即

$f_q(x_{m}, m) = f_{q}(q_m,m) =  (W_{q}x_{m})e^{im\theta}$

两个向量间的内积就可以表达为

$$
<f_q(x_{m}, m),f_k(x_{n}, n)> = Re[W_{q}x_{m}(W_{k}x_{n})^{*}e^{i(m-n)\theta}]
$$

可以看到，相对位置信息得到了很好的表示。

LLaMA 里的实现

```python
def precompute_freqs_cis(dim: int, seq_len: int, theta: float = 10000.0):
    # 计算词向量元素两两分组之后，每组元素对应的旋转角度
    freqs = 1.0 / (theta ** (torch.arange(0, dim, 2)[: (dim // 2)].float() / dim))
    # 生成 token 序列索引 t = [0, 1,..., seq_len-1]
    t = torch.arange(seq_len, device=freqs.device)
    # freqs.shape = [seq_len, dim // 2] 
    freqs = torch.outer(t, freqs).float()
    # 计算结果是个复数向量，输入分别是模和角度，这里模设置为1
    # 假设 freqs = [x, y]
    # 则 freqs_cis = [cos(x) + sin(x)i, cos(y) + sin(y)i]
    # 可以看出每一个元素就是 e^{im\theta}
    freqs_cis = torch.polar(torch.ones_like(freqs), freqs)
    return freqs_cis

def apply_rotary_emb(
    xq: torch.Tensor,
    xk: torch.Tensor,
    freqs_cis: torch.Tensor,
) -> Tuple[torch.Tensor, torch.Tensor]:
    # 两两分组，每组内部表示为复数的实部和虚部
    # xq.shape = [batch_size, seq_len, dim]
    # xq_.shape = [batch_size, seq_len, dim // 2, 2]
    xq_ = xq.float().reshape(*xq.shape[:-1], -1, 2)
    xk_ = xk.float().reshape(*xk.shape[:-1], -1, 2)

    # 转为复数域，变成 [batch_size, seq_len, dim//2]
    xq_ = torch.view_as_complex(xq_)
    xk_ = torch.view_as_complex(xk_)

    # 应用旋转操作，然后将结果转回实数域
    # 元素对应相乘
    # xq_out.shape = [batch_size, seq_len, dim]
    xq_out = torch.view_as_real(xq_ * freqs_cis).flatten(2)
    xk_out = torch.view_as_real(xk_ * freqs_cis).flatten(2)
    return xq_out.type_as(xq), xk_out.type_as(xk)
```

### 原理之外

1.  为什么dim 维度可以两两配对组合成复数的实部和虚部？有什么隐层含义？
2.  为什么只对 q 和 k 做，v没有加位置信息。
3.  经典使用 Sinusoidal 函数计算位置编码的是怎么做的？RoPE 相比有什么优势？

1.  Sinusoidal 函数的表达式 ![image.png](/img/yuque/计算机/大模型/1._模型调研/1.3_大模型逐个学习笔记/assets/LLaMA_家族-3.png)
2.  Sinusoidal 之前为什么有效？

1.  具有**相对位置表达能力**：Sinusoidal可以学习到相对位置，对于固定位置距离的k，PE(i+k)可以表示成PE(i)的线性函数。
2.  两个**位置向量的内积只和相对位置 k 有关**。
3.  Sinusoidal编码具有**对称性**。PE(t+k)PE(t) = PE(t)PE(t-k)
4.  随着k的增加，内积的结果会直接减少，即会存在**远程衰减**。

3.  为什么是 10000？（周期大，一般文本长度内编码不会重复）
4.  缺点是什么？

1.  理论很美好，PE 看着不错，但是和 token embedding 相加再乘以映射矩阵后，就不具备上述优势了（没有对称性、远程衰减）

5.  什么是好的位置编码？

1.  每个位置输出一个唯一的编码
2.  具备良好的外推性
3.  任何位置之间的相对距离在不同长度的句子中应该是一致的

6.  RoPE 的优点：

1.  绝对位置编码
2.  乘性的，且可以准确表达相对位置
3.  具有一定的外推性

4.  RoPE 的问题：

1.  虽然RoPE理论上可以编码任意长度的绝对位置信息，并且sin/cos计算就能将任意长度的相对位置信息表达出来，但是实验发现RoPE仍然存在**外推问题**，即测试长度超过训练长度之后，模型的效果会有显著的崩坏，具体表现为**困惑度（Perplexity，PPL）**等指标显著上升

5.  RoPE 的改进：

1.  [位置线性内插（Position Interpolation，PI）](https://arxiv.org/abs/2306.15595):

1.  为了解决模型处理更长序列时的问题，研究者们提出了一个策略：**将位置索引本身进行下采样或缩放**。具体来说，他们将原本在[0, 4096]范围内索引缩放到[0, 2048]（预训练的范围）的范围内。通过这种方式，所有的位置索引都被映射回了模型预训练时的范围内，从而帮助模型更好地处理这些原本超出其处理能力的输入序列。
2.  公式：![image.png](/img/yuque/计算机/大模型/1._模型调研/1.3_大模型逐个学习笔记/assets/LLaMA_家族-4.png)
3.  对照组是使用10000条样本，直接进行微调长度为8192的文本，外推长度只能到2560；而只用1000条样本，在**微调（内插是要微调的）**中使用位置线性内插，外推长度就可以达到8192。外推适应性更强。
4.  ![image.png](/img/yuque/计算机/大模型/1._模型调研/1.3_大模型逐个学习笔记/assets/LLaMA_家族-5.png)

2.  NTK-Aware Scaled RoPE（非线性内插）：

1.  插值方法是线性的，这种方法在处理相近的token时可能无法准确区分它们的顺序和位置。毕竟缩放后离得太近了。
2.  非线性内插是改变RoPE的base而不是比例，从而改变每个RoPE维度向量与下一个向量的“旋转”速度。
3.  它允许LLaMA模型在无需任何微调且困惑度降低到最小的情况下扩展上下文长度。这种方法基于Neural Tangent Kernel（NTK）理论和RoPE插值进行改进。
4.  公式 ![image.png](/img/yuque/计算机/大模型/1._模型调研/1.3_大模型逐个学习笔记/assets/LLaMA_家族-6.png)
5.  这个方法是从进制角度看的，我们对10进制数字 n 求 $\beta$ 进制表示的（从右向左第m位数字），方法是$[\frac{n}{\beta^{m-1}}]mod\beta$。要是把 $\beta = 10000^{m/d}$ ，往上一带，看着还是很像的。所以RoPE / Sinusoidal位置编码类比为位置n的 𝛽 进制编码。建立起这个联系后，直接外推方案就是啥也不改，位置线性内插就是将n换成n/k，其中k是要扩大的倍数。
6.  从提出者这个思想来看，**任意能实现“高频外推、低频内插”的方案都是可以的，并非只有上述引入λ的方案**。
7.  **如果从前面的进制转换的角度分析**，直接外推会将外推压力集中在“高位（m较大）”上，而位置内插则会将“低位（m较小）”的表示变得更加稠密，不利于区分相对距离。NTK-aware Scaled RoPE其实就是进制转换，它将外推压力平摊到每一位上，并且保持相邻间隔不变，这些特性对明显更倾向于依赖相对位置的LLM来说是非常友好和关键的，所以它可以不微调也能实现一定的效果。

## Alibi

> [https://arxiv.org/pdf/2108.12409](https://arxiv.org/pdf/2108.12409)

前面说过，在 q 和 k 乘 w 后再算 self-att，之前的 sin 位置编码就丧失了远程衰减等优秀特性。因此 RoPE 是从引入乘性位置表示来解决的，那另外一个思路就是 alibi 的在算 softmax 之前，q k 相乘后 做。这样也能避免 sin 位置编码的问题。论文里表示可以加快11%的训练速度，以及减少11%的内存使用。

具体原理是在 q\_m 和k\_n 相乘后，再加上他们之间的相对位置差(n-m）\* 坡度

![image.png](/img/yuque/计算机/大模型/1._模型调研/1.3_大模型逐个学习笔记/assets/LLaMA_家族-7.png)

需要注意的是：

-   论文用在因果编码中，因此 n <= m
-   坡度：每个 head 独一无二的数，比如 8 个 head 时，分别为 **1/2, 1/4, 1/8, 1/16, 1/32, 1/64, 1/128, 1/256。**

-   用途是啥？为啥每个 head 都要不一样？为什么要这么设？

-   每个 head 都有一个自己的 m
-   决定了这个偏差随着查询（query）和键（key）之间距离的增加而增加的速率。我理解就是强制指定每个 head 关注不同的距离敏感度。

效果

![image.png](/img/yuque/计算机/大模型/1._模型调研/1.3_大模型逐个学习笔记/assets/LLaMA_家族-8.png)

# Tokenizer

详见 [深入理解NLP Subword算法：BPE、WordPiece、ULM](https://zhuanlan.zhihu.com/p/86965595)

![image.png](/img/yuque/计算机/大模型/1._模型调研/1.3_大模型逐个学习笔记/assets/LLaMA_家族-9.png)

## BPE

算法流程为

-   准备足够大的训练语料
-   确定期望的subword词表大小
-   将单词拆分为字符序列并在末尾添加后缀“ </ w>”，统计单词频率。 本阶段的subword的粒度是字符。 例如，“ low”的频率为5，那么我们将其改写为“ l o w </ w>”：5
-   统计每一个连续字节对的出现频率，选择最高频者合并成新的subword
-   重复第4步直到达到第2步设定的subword词表大小或下一个最高频的字节对出现频率为1

停止符""的意义在于表示subword是词后缀。举例来说："st"字词不加""可以出现在词首如"st ar"，加了""表明改字词位于词尾，如"wide st"，二者意义截然不同。

每次合并后词表可能出现3种变化：

-   +1，表明加入合并后的新字词，同时原来在2个子词还保留（2个字词不是完全同时连续出现）
-   +0，表明加入合并后的新字词，同时原来2个子词中一个保留，一个被消解（一个字词完全随着另一个字词的出现而紧跟着出现）
-   \-1，表明加入合并后的新字词，同时原来2个子词都被消解（2个字词同时连续出现）

实际上，随着合并的次数增加，词表大小通常先**增加后减小。**

例子：

```c
输入：
{'l o w </w>': 5, 'l o w e r </w>': 2, 'n e w e s t </w>': 6, 'w i d e s t </w>': 3}
Iter 1, 最高频连续字节对"e"和"s"出现了6+3=9次，合并成"es"。输出：
{'l o w </w>': 5, 'l o w e r </w>': 2, 'n e w es t </w>': 6, 'w i d es t </w>': 3}
Iter 2, 最高频连续字节对"es"和"t"出现了6+3=9次, 合并成"est"。输出：
{'l o w </w>': 5, 'l o w e r </w>': 2, 'n e w est </w>': 6, 'w i d est </w>': 3}
Iter 3, 以此类推，最高频连续字节对为"est"和"</w>" 输出：
{'l o w </w>': 5, 'l o w e r </w>': 2, 'n e w est</w>': 6, 'w i d est</w>': 3}
……
Iter n, 继续迭代直到达到预设的subword词表大小或下一个最高频的字节对出现频率为1。
```

给定单词序列

[“the”, “highest”, “mountain”]

假设已有排好序的subword词表

[“errrr”, “tain”, “moun”, “est”, “high”, “the”, “a”]

则编码结果为

"the" -> ["the"]  
"highest" -> ["high", "est"]  
"mountain" -> ["moun", "tain"]

## WordPiece

WordPiece算法可以看作是BPE的变种。不同点在于，WordPiece基于概率生成新的subword而不是下一最高频字节对。

有点抽象。

1.  准备足够大的训练语料
2.  确定期望的subword词表大小
3.  将单词拆分成字符序列
4.  基于第3步数据训练语言模型
5.  从所有可能的subword单元中选择加入语言模型后能最大程度地增加训练数据概率的单元作为新的单元
6.  重复第5步直到达到第2步设定的subword词表大小或概率增量低于某一阈值

## ULM

ULM是另外一种subword分隔算法，它能够输出带概率的多个子词分段。它引入了一个假设：所有subword的出现都是独立的，并且subword序列由subword出现概率的乘积产生。WordPiece和ULM都利用语言模型建立subword词表。

1.  准备足够大的训练语料
2.  确定期望的subword词表大小
3.  给定词序列优化下一个词出现的概率
4.  计算每个subword的损失
5.  基于损失对subword排序并保留前X%。为了避免OOV，建议保留字符级的单元
6.  重复第3至第5步直到达到第2步设定的subword词表大小或第5步的结果不再变化

# LLAMA 和 LLAMA2 的区别

1.  更强大的数据清洗：语料库包括来自公开可用来源的新数据混合，不包括 Meta 的产品或服务的数据。已从某些已知包含大量个人信息的网站中删除数据。
2.  总token数量增加了40%：训练使用了2 万亿个token的数据，这在性能和成本之间提供了良好的平衡，通过对最真实的来源进行过采样，以增加知识并减少幻觉。
3.  加倍上下文长度：更长的上下文窗口使模型能够处理更多信息，这对于支持聊天应用中的更长历史记录、各种摘要任务和理解更长文档特别有用。
4.  Grouped-query attention （GQA）：这是一种方法，允许在多头注意力（MHA）模型中共享键和值投影，从而减少与缓存相关的内存成本。通过使用 GQA，更大的模型可以在优化内存使用的同时保持性能。
5.  专注于收集高质量的 SFT 示例，这显著改善了结果。研究发现，有限的一组干净的指令调整数据足以达到高质量水平。作者观察到，从结果 SFT 模型中采样的输出通常与人工注释员手写的 SFT 数据具有竞争力，这表明注释工作可以重新分配给基于偏好的注释用于 RLHF。
6.  提出了一种新的方法 Ghost Attention（GAtt），它帮助模型在多个回合中控制对话流程。

# LLAMA3 和 LLAMA2 的区别

1.  更多的 token 数，15T
2.  分词器**和词汇表**：Llama 3 引入了一个具有128K标记的分词器，这比前代模型的词汇表更大，从而使得模型能够更精确地编码语言信息，显著提升了性能。
3.  分组查询注意力（GQA）：增强模型的推理效率，可以提高处理长距离依赖关系时的效率
4.  序列长度和子午注意力：LLAMA3 在 8192 个 token 序列上进行训练，利用掩码机制确保自我注意力操作不会跨越文档边界，这有助于模型更好地处理长文本数据。
{% endraw %}
