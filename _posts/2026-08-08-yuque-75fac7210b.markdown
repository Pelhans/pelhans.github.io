---
title: "141\\. 环形链表"
date: 2026-08-08
layout: post
liquid: false
tags: [语雀, NEXT]
categories: [NEXT]
yuque: true
---

{% raw %}
# 141\. 环形链表

![image.png](/img/yuque/NEXT/assets/2._代码刷题-1.png)

```python
class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        if not head or not head.next:
            return False
        pfast = head.next
        p_slow = head
        while pfast != p_slow:
            if not pfast or not pfast.next:
                return False
            pfast = pfast.next.next
            p_slow = p_slow.next
        return True
```

# 142\. 环形链表II

![image.png](/img/yuque/NEXT/assets/2._代码刷题-2.png)

```python
class Solution:
    def detectCycle(self, head: Optional[ListNode]) -> Optional[ListNode]:
        slow = fast = head
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
            if fast is slow:
                while slow is not head:
                    slow = slow.next
                    head = head.next
                return slow
        return None
```

# 146\. LRU缓存

![image.png](/img/yuque/NEXT/assets/2._代码刷题-3.png)

```python
class DLinkedNode:
    def __init__(self, key=0, value=0):
        self.key = key
        self.value = value
        self.prev = None
        self.next = None

class LRUCache:

    def __init__(self, capacity: int):
        self.cache = dict()
        # 使用伪头部和伪尾部节点    
        self.head = DLinkedNode()
        self.tail = DLinkedNode()
        self.head.next = self.tail
        self.tail.prev = self.head
        self.capacity = capacity
        self.size = 0

    def get(self, key: int) -> int:
        if key not in self.cache:
            return -1
        # 如果 key 存在，先通过哈希表定位，再移到头部
        node = self.cache[key]
        self.moveToHead(node)
        return node.value

    def put(self, key: int, value: int) -> None:
        if key not in self.cache:
            # 如果 key 不存在，创建一个新的节点
            node = DLinkedNode(key, value)
            # 添加进哈希表
            self.cache[key] = node
            # 添加至双向链表的头部
            self.addToHead(node)
            self.size += 1
            if self.size > self.capacity:
                # 如果超出容量，删除双向链表的尾部节点
                removed = self.removeTail()
                # 删除哈希表中对应的项
                self.cache.pop(removed.key)
                self.size -= 1
        else:
            # 如果 key 存在，先通过哈希表定位，再修改 value，并移到头部
            node = self.cache[key]
            node.value = value
            self.moveToHead(node)
    
    def addToHead(self, node):
        node.prev = self.head
        node.next = self.head.next
        self.head.next.prev = node
        self.head.next = node
    
    def removeNode(self, node):
        node.prev.next = node.next
        node.next.prev = node.prev

    def moveToHead(self, node):
        self.removeNode(node)
        self.addToHead(node)

    def removeTail(self):
        node = self.tail.prev
        self.removeNode(node)
        return node
```

# 148\. 排序链表

![image.png](/img/yuque/NEXT/assets/2._代码刷题-4.png)

```python
class Solution:
    def sortList(self, head: ListNode) -> ListNode:
        def sortFunc(head: ListNode, tail: ListNode) -> ListNode:
            if not head:
                return head
            if head.next == tail:
                head.next = None
                return head
            slow = fast = head
            while fast != tail:
                slow = slow.next
                fast = fast.next
                if fast != tail:
                    fast = fast.next
            mid = slow
            return merge(sortFunc(head, mid), sortFunc(mid, tail))
            
        def merge(head1: ListNode, head2: ListNode) -> ListNode:
            dummyHead = ListNode(0)
            temp, temp1, temp2 = dummyHead, head1, head2
            while temp1 and temp2:
                if temp1.val <= temp2.val:
                    temp.next = temp1
                    temp1 = temp1.next
                else:
                    temp.next = temp2
                    temp2 = temp2.next
                temp = temp.next
            if temp1:
                temp.next = temp1
            elif temp2:
                temp.next = temp2
            return dummyHead.next
        
        return sortFunc(head, None)
```

# 152\. 乘积最大子数组

![image.png](/img/yuque/NEXT/assets/2._代码刷题-5.png)

```python
class Solution:
    def maxProduct(self, nums: List[int]) -> int:
        if not nums: return 
        res = nums[0]
        pre_max = nums[0]
        pre_min = nums[0]
        for num in nums[1:]:
            cur_max = max(pre_max * num, pre_min * num, num)
            cur_min = min(pre_max * num, pre_min * num, num)
            res = max(res, cur_max)
            pre_max = cur_max
            pre_min = cur_min
        return res
```

# 155\. 最小栈

![image.png](/img/yuque/NEXT/assets/2._代码刷题-6.png)

```python
class MinStack:
    def __init__(self):
        self.stack = []
        self.min_stack = [math.inf]

    def push(self, x: int) -> None:
        self.stack.append(x)
        self.min_stack.append(min(x, self.min_stack[-1]))

    def pop(self) -> None:
        self.stack.pop()
        self.min_stack.pop()

    def top(self) -> int:
        return self.stack[-1]

    def getMin(self) -> int:
        return self.min_stack[-1]
```

# 160\. 相交链表

![image.png](/img/yuque/NEXT/assets/2._代码刷题-7.png)

```python
class Solution:
    def getIntersectionNode(self, headA: ListNode, headB: ListNode) -> Optional[ListNode]:
        A, B = headA, headB
        while A != B:
            A = A.next if A else headB
            B = B.next if B else headA
        return A
```

# 169\. 多数元素

![image.png](/img/yuque/NEXT/assets/2._代码刷题-8.png)

```python
class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        count = 0
        candidate = None

        for num in nums:
            if count == 0:
                candidate = num
            count += (1 if num == candidate else -1)

        return candidate
```

# 198\. 打家劫舍

![image.png](/img/yuque/NEXT/assets/2._代码刷题-9.png)

```python
class Solution:
    def rob(self, nums: List[int]) -> int:
        if not nums:
            return 0

        size = len(nums)
        if size == 1:
            return nums[0]
        
        dp = [0] * size
        dp[0] = nums[0]
        dp[1] = max(nums[0], nums[1])
        for i in range(2, size):
            dp[i] = max(dp[i - 2] + nums[i], dp[i - 1])
        
        return dp[size - 1]
```

# 200\. 岛屿数量

![image.png](/img/yuque/NEXT/assets/2._代码刷题-10.png)

```python
class Solution:
    def numIslands(self, grid: List[List[str]]) -> int:
        def dfs(grid, i, j):
            if not 0 <= i < len(grid) or not 0 <= j < len(grid[0]) or grid[i][j] != '1': 
                return
            grid[i][j] = '2'
            dfs(grid, i + 1, j)
            dfs(grid, i, j + 1)
            dfs(grid, i - 1, j)
            dfs(grid, i, j - 1)
        count = 0
        for i in range(len(grid)):
            for j in range(len(grid[0])):
                if grid[i][j] == '1':
                    dfs(grid, i, j)
                    count += 1
        return count
```

# 206\. 反转链表

![image.png](/img/yuque/NEXT/assets/2._代码刷题-11.png)

```python
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        cur = head
        prev = None
        while cur:
            next = cur.next
            cur.next = prev
            prev = cur
            cur = next
        return prev
```

# 207\. 课程表

![image.png](/img/yuque/NEXT/assets/2._代码刷题-12.png)

```python
class Solution:
    def canFinish(self, numCourses: int, prerequisites: List[List[int]]) -> bool:
        edges = collections.defaultdict(list)
        indeg = [0] * numCourses

        for info in prerequisites:
            edges[info[1]].append(info[0])
            indeg[info[0]] += 1
        
        q = collections.deque([u for u in range(numCourses) if indeg[u] == 0])
        visited = 0

        while q:
            visited += 1
            u = q.popleft()
            for v in edges[u]:
                indeg[v] -= 1
                if indeg[v] == 0:
                    q.append(v)

        return visited == numCourses
```

# 208\. 实现 Trie (前缀树)

![image.png](/img/yuque/NEXT/assets/2._代码刷题-13.png)

```python
class Trie:
    def __init__(self):
        self.children = [None] * 26
        self.isEnd = False
    
    def searchPrefix(self, prefix: str) -> "Trie":
        node = self
        for ch in prefix:
            ch = ord(ch) - ord("a")
            if not node.children[ch]:
                return None
            node = node.children[ch]
        return node

    def insert(self, word: str) -> None:
        node = self
        for ch in word:
            ch = ord(ch) - ord("a")
            if not node.children[ch]:
                node.children[ch] = Trie()
            node = node.children[ch]
        node.isEnd = True

    def search(self, word: str) -> bool:
        node = self.searchPrefix(word)
        return node is not None and node.isEnd

    def startsWith(self, prefix: str) -> bool:
        return self.searchPrefix(prefix) is not None
```

# 215\. 数组中的第K个最大元素

![image.png](/img/yuque/NEXT/assets/2._代码刷题-14.png)

```python
class Solution:
    def findKthLargest(self, nums: List[int], k: int) -> int:
        pq = []   # 将数组加入小顶堆，堆中维护当前值最大的k个数
        for num in nums:
            heapq.heappush(pq, num) # 当前元素入堆
            if len(pq) > k:
                heapq.heappop(pq)   # 堆中元素超过k个，弹出最小的那个
        return pq[0]    # 最后堆顶的即为第k大的数
```

# 221\. 最大正方形

![image.png](/img/yuque/NEXT/assets/2._代码刷题-15.png)

```python
class Solution:
    def maximalSquare(self, matrix: List[List[str]]) -> int:
        if len(matrix) == 0 or len(matrix[0]) == 0:
            return 0
        
        maxSide = 0
        rows, columns = len(matrix), len(matrix[0])
        dp = [[0] * columns for _ in range(rows)]
        for i in range(rows):
            for j in range(columns):
                if matrix[i][j] == '1':
                    if i == 0 or j == 0:
                        dp[i][j] = 1
                    else:
                        dp[i][j] = min(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1]) + 1
                    maxSide = max(maxSide, dp[i][j])
        
        maxSquare = maxSide * maxSide
        return maxSquare
```

# 226\. 翻转二叉树

![image.png](/img/yuque/NEXT/assets/2._代码刷题-16.png)

```python
class Solution:
    def invertTree(self, root: Optional[TreeNode]) -> Optional[TreeNode]:
        if not root:
            return root
        left = self.invertTree(root.left)
        right = self.invertTree(root.right)
        root.left = right
        root.right = left
        return root
```

# 234\. 回文链表

![image.png](/img/yuque/NEXT/assets/2._代码刷题-17.png)

```python
class Solution:
    def isPalindrome(self, head: Optional[ListNode]) -> bool:
        if head is None:
            return True

        # 找到前半部分链表的尾节点并反转后半部分链表
        first_half_end = self.end_of_first_half(head)
        second_half_start = self.reverse_list(first_half_end.next)

        # 判断是否回文
        result = True
        first_position = head
        second_position = second_half_start
        while result and second_position is not None:
            if first_position.val != second_position.val:
                result = False
            first_position = first_position.next
            second_position = second_position.next

        # 还原链表并返回结果
        first_half_end.next = self.reverse_list(second_half_start)
        return result    

    def end_of_first_half(self, head):
        fast = head
        slow = head
        while fast.next is not None and fast.next.next is not None:
            fast = fast.next.next
            slow = slow.next
        return slow

    def reverse_list(self, head):
        previous = None
        current = head
        while current is not None:
            next_node = current.next
            current.next = previous
            previous = current
            current = next_node
        return previous
```

# 236\. 二叉树的最近公共祖先

![image.png](/img/yuque/NEXT/assets/2._代码刷题-18.png)

```python
class Solution:
    def lowestCommonAncestor(self, root: TreeNode, p: TreeNode, q: TreeNode) -> TreeNode:
        if not root or root == p or root == q:
            return root
        left = self.lowestCommonAncestor(root.left, p, q)
        right = self.lowestCommonAncestor(root.right, p, q)
        if not left:
            return right
        if not right:
            return left
        return root
```

# 238\. 除自身以外数组的乘积

![image.png](/img/yuque/NEXT/assets/2._代码刷题-19.png)

```python
class Solution:
    def productExceptSelf(self, nums: List[int]) -> List[int]:
        ans, tmp = [1] * len(nums), 1
        for i in range(1, len(nums)):
            ans[i] = ans[i - 1] * nums[i - 1] # 下三角
        for i in range(len(nums) - 2, -1, -1):
            tmp *= nums[i + 1]                # 上三角
            ans[i] *= tmp                     # 下三角 * 上三角
        return ans
```

# 739\. 每日温度

![image.png](/img/yuque/NEXT/assets/2._代码刷题-20.png)

```python
class Solution:
    def dailyTemperatures(self, temperatures: List[int]) -> List[int]:
        length = len(temperatures)
        ans = [0] * length
        stack = []
        for i in range(length):
            temperature = temperatures[i]
            while stack and temperature > temperatures[stack[-1]]:
                prev_index = stack.pop()
                ans[prev_index] = i - prev_index
            stack.append(i)
        return ans
```

  

# 剑指

## 剑指 Offer 03. 数组中重复的数字 LCR120

找出数组中重复的数字。

在一个长度为 n 的数组 nums 里的所有数字都在 0～n-1 的范围内。数组中某些数字是重复的，但不知道有几个数字重复了，也不知道每个数字重复了几次。请找出数组中任意一个重复的数字。

![image.png](/img/yuque/NEXT/assets/2._代码刷题-21.png)

思路：充分利用给定数组的特性

```python
class Solution:
    def findRepeatDocument(self, documents: List[int]) -> int:
        if len(documents) <= 1:  
            return None  
        cur = 0  
        while cur < len(documents):  
            while documents[cur] != cur:  
                if documents[cur] == documents[documents[cur]]:  
                    return documents[cur]  
                documents[documents[cur]], documents[cur] = documents[cur], documents[documents[cur]]  
            cur += 1  
        return None  
```

​  

# 剑指 Offer 04. 二维数组中的查找

  

在一个 n \* m 的二维数组中，每一行都按照从左到右递增的顺序排序，每一列都按照从上到下递增的顺序排序。请完成一个函数，输入这样的一个二维数组和一个整数，判断数组中是否含有该整数。

![image.png](/img/yuque/NEXT/assets/2._代码刷题-22.png)

​  

思路：变相的二分查找

​  

```python
class Solution:  
    def findNumberIn2DArray(self, matrix: List[List[int]], target: int) -> bool:  
        if not matrix or not matrix[0]:  
            return False  
        row = 0  
        col = len(matrix[0]) - 1  

        while row < len(matrix) and col >= 0:  
            if matrix[row][col] == target:  
                return True  
            elif matrix[row][col] < target:  
                row += 1  
            else:  
                col -= 1  
        return False  
```

​  

# 剑指 Offer 05. 替换空格

请实现一个函数，把字符串s中的每个空格替换成"%20"。

```python
class Solution:  
 def replaceSpace(self, s: str) -> str:  
     if not s:  
         return ""  
     res = []  
     for i in range(len(s)):  
         if s[i] == " ":  
             res.append("%20")  
         else:  
             res.append(s[i])  
     return "".join(res)  
```

# 剑指 Offer 06. 从尾到头打印链表

输入一个链表的头节点，从尾到头反过来返回每个节点的值（用数组返回）。

```python
class Solution:  
    def reversePrint(self, head: ListNode) -> List[int]:  
        if not head:  
            return []  
        res = []  
        while head:  
            res.append(head.val)  
            head = head.next  
        return res[::-1]
```

# 剑指 Offer 07. 重建二叉树

输入某二叉树的前序遍历和中序遍历的结果，请重建该二叉树。假设输入的前序遍历和中序遍历的结果中都不含重复的数字。

```python
class Solution:  
    def buildTree(self, preorder: List[int], inorder: List[int]) -> TreeNode:  
        if not preorder:  
            return None  
        root = TreeNode(preorder[0])  
        index = inorder.index(preorder[0])  
        root.left = self.buildTree(preorder[1:index+1], inorder[:index])  
        root.right = self.buildTree(preorder[index+1:], inorder[index+1:])  
        return root
```

# 剑指 Offer 09. 用两个栈实现队列

用两个栈实现一个队列。队列的声明如下，请实现它的两个函数 appendTail 和 deleteHead ，分别完成在队列尾部插入整数和在队列头部删除整数的功能。(若队列中没有元素，deleteHead 操作返回 -1 )

1. **class** CQueue:

2.

3. **def** \_\_init\_\_(self):

4. self.stack1 = []

5. self.stack2 = []

6.

7. **def** appendTail(self, value: int) -> None:

8. self.stack1.append(value)

9.

10. **def** deleteHead(self) -> int:

11. **if** self.stack2:

12. **return** self.stack2.pop()

13. **elif** self.stack1:

14. self.stack2 = self.stack1[::-1]

15. self.stack1 = []

16. **return** self.stack2.pop()

17. **else**:

18. **return** -1

# 剑指 Offer 10- I. 斐波那契数列

写一个函数，输入 n ，求斐波那契（Fibonacci）数列的第 n 项。斐波那契数列的定义如下：

​  

1. **class** Solution:

2. **def** fib(self, n: int) -> int:

3. **if** n <= 0:

4. **return** 0

5. first = 0

6. second = 1

7. res = 1

8. **for** i **in** range(2, n+1):

9. res = first + second

10. first = second

11. second = res

12. **return** res%1000000007

# 剑指 Offer 10- II. 青蛙跳台阶问题

一只青蛙一次可以跳上1级台阶，也可以跳上2级台阶。求该青蛙跳上一个 n 级的台阶总共有多少种跳法。答案需要取模 1e9+7（1000000007），如计算初始结果为：1000000008，请返回 1。

​  

1. **class** Solution:

2. **def** numWays(self, n: int) -> int:

3. **if** n <= 0:

4. **return** 1

5. **if** n == 1:

6. **return** 1

7. **if** n == 2:

8. **return** 2

9. first = 1

10. second = 2

11. res = 0

12. **for** i **in** range(3, n+1):

13. res = first + second

14. first = second

15. second = res

16. **return** res%1000000007

# 剑指 Offer 11. 旋转数组的最小数字（不熟）

把一个数组最开始的若干个元素搬到数组的末尾，我们称之为数组的旋转。输入一个递增排序的数组的一个旋转，输出旋转数组的最小元素。例如，数组 [3,4,5,1,2] 为 [1,2,3,4,5] 的一个旋转，该数组的最小值为1。

​  

​  

1. **class** Solution:

2. **def** minArray(self, numbers: List[int]) -> int:

3. start = 0

4. end = len(numbers) - 1

5. n = len(numbers) - 1

6. **while** start < end:

7. mid = (start+end)//2

8. **if** numbers[mid] > numbers[end]:

9. start = mid + 1

10. **elif** numbers[mid] < numbers[end]:

11. end = mid

12. **else**:

13. end -= 1

14. **return** numbers[start]

# 剑指 Offer 12. 矩阵中的路径

请设计一个函数，用来判断在一个矩阵中是否存在一条包含某字符串所有字符的路径。路径可以从矩阵中的任意一格开始，每一步可以在矩阵中向左、右、上、下移动一格。如果一条路径经过了矩阵的某一格，那么该路径不能再次进入该格子。例如，在下面的3×4的矩阵中包含一条字符串“bfce”的路径（路径中的字母用加粗标出）。

​  

​  

1. **class** Solution:

2. **def** exist(self, board: List[List\[str]\], word: str) -> bool:

3. **if** **not** word:

4. **return** True

5. **if** **not** board **or** **not** board[0]:

6. **return** False

7. stack = []

8. m = len(board)

9. n = len(board[0])

10. **for** i **in** range(m):

11. **for** j **in** range(n):

12. **if** board[i][j] == word[0]:

13. stack.append([i, j])

14.

15. **def** back(row, col, word, \_board):

16. **if** len(word) == 0:

17. **return** True

18. \# 看上下左右

19. **if** row - 1 >= 0 **and** board[row-1][col] == word[0]:

20. \_board[row-1][col] = "USED"

21. **if** back(row-1, col, word[1:], \_board):

22. **return** True

23. \_board[row-1][col] = word[0]

24. **if** row + 1 < m **and** board[row+1][col] == word[0]:

25. \_board[row+1][col] = "USED"

26. **if** back(row+1, col, word[1:], \_board):

27. **return** True

28. \_board[row+1][col] = word[0]

29. **if** col - 1 >= 0 **and** board[row][col-1] == word[0]:

30. \_board[row][col-1] = "USED"

31. **if** back(row, col-1, word[1:], \_board):

32. **return** True

33. \_board[row][col-1] = word[0]

34. **if** col + 1 < n **and** board[row][col+1] == word[0]:

35. \_board[row][col+1] = "USED"

36. **if** back(row, col+1, word[1:], \_board):

37. **return** True

38. \_board[row][col+1] = word[0]

39. **return** False

40.

41. **for** row, col **in** stack:

42. board[row][col] = "USED"

43. **if** back(row, col, word[1:], board):

44. **return** True

45. board[row][col] = word[0]

46. **return** False

# 剑指 Offer 13. 机器人的运动范围

地上有一个m行n列的方格，从坐标 [0,0] 到坐标 [m-1,n-1] 。一个机器人从坐标 [0, 0] 的格子开始移动，它每次可以向左、右、上、下移动一格（不能移动到方格外），也不能进入行坐标和列坐标的数位之和大于k的格子。例如，当k为18时，机器人能够进入方格 [35, 37] ，因为3+5+3+7=18。但它不能进入方格 [35, 38]，因为3+5+3+8=19。请问该机器人能够到达多少个格子？

​  

思路：BFS，用栈解决

​  

1. **class** Solution:

2. **def** movingCount(self, m: int, n: int, k: int) -> int:

3. res = self.\_check\_dight\_sum(35, 38, 18)

4. res = 1

5. stack = [\[0,0]\]

6. matrix = [\[True]\*n **for** \_ **in** range(m+1)\]

7. matrix[0][0] = False

8. **while** stack:

9. \_tmp = []

10. **for** row, col **in** stack:

11. **if** row - 1 >= 0 **and** matrix[row-1][col] **and** self.\_check\_dight\_sum(row-1, col, k):

12. res += 1

13. \_tmp.append([row-1, col])

14. matrix[row-1][col] = False

15. **if** row + 1 < m **and** matrix[row+1][col] **and** self.\_check\_dight\_sum(row+1, col, k):

16. res += 1

17. \_tmp.append([row+1, col])

18. matrix[row+1][col] = False

19. **if** col - 1 >= 0 **and** matrix[row][col-1] **and** self.\_check\_dight\_sum(row, col-1, k):

20. res += 1

21. \_tmp.append([row, col-1])

22. matrix[row][col-1] = False

23. **if** col + 1 < n **and** matrix[row][col+1] **and** self.\_check\_dight\_sum(row, col+1, k):

24. res += 1

25. \_tmp.append([row, col+1])

26. matrix[row][col+1] = False

27. stack = \_tmp

28. **return** res

29.

30. **def** \_check\_dight\_sum(self, num1, num2, k):

31. res = 0

32. **while** num1:

33. res += num1%10

34. num1 //= 10

35. **while** num2:

36. res += num2%10

37. num2 //= 10

38. **if** res <= k:

39. **return** True

40. **return** False

# 剑指 Offer 14- I. 剪绳子

给你一根长度为 n 的绳子，请把绳子剪成整数长度的 m 段（m、n都是整数，n>1并且m>1），每段绳子的长度记为 k[0],k[1]...k[m-1] 。请问 k[0]\*k[1]\*...\*k[m-1] 可能的最大乘积是多少？例如，当绳子的长度是8时，我们把它剪成长度分别为2、3、3的三段，此时得到的最大乘积是18。

​  

1. **class** Solution:

2. **def** cuttingRope(self, n: int) -> int:

3. dp = [1 **for** i **in** range(n+1)]

4. dp[0] = 1

5. dp[1] = 1

6. dp[2] = 1

7. **for** i **in** range(3, n+1):

8. **for** j **in** range(1, i):

9. dp[i] = max(dp[i], max((i-j)\*j, dp[i-j]\*j))

10. **return** dp[n]

# 剑指 Offer 15. 二进制中1的个数

​  

请实现一个函数，输入一个整数，输出该数二进制表示中 1 的个数。例如，把 9 表示成二进制是 1001，有 2 位是 1。因此，如果输入 9，则该函数输出 2。

​  

1. **class** Solution:

2. **def** hammingWeight(self, n: int) -> int:

3. res = 0

4. **while** n:

5. res += n & 1

6. n >>= 1

7. **return** res

# 剑指 Offer 16. 数值的整数次方

实现函数double Power(double base, int exponent)，求base的exponent次方。不得使用库函数，同时不需要考虑大数问题。

思路：二分法

1. **class** Solution:

2. **def** myPow(self, x: float, n: int) -> float:

3. **if** n == 0:

4. **return** 1

5. sign = 1 **if** n > 0 **else** -1

6. **if** sign == 1:

7. **return** self.\_pow(x, abs(n))

8. **return** 1/self.\_pow(x, abs(n))

9.

10. **def** \_pow(self, x, n):

11. **if** n == 0:

12. **return** 1

13. v = self.\_pow(x, n//2)

14. **if** n%2 == 0:

15. **return** v \* v

16. **else**:

17. **return** v \* v \* x

# 剑指 Offer 17. 打印从1到最大的n位数

输入数字 n，按顺序打印出从 1 到最大的 n 位十进制数。比如输入 3，则打印出 1、2、3 一直到最大的 3 位数 999。

​  

1. **class** Solution:

2. **def** printNumbers(self, n: int) -> List[int]:

3. **return** [i **for** i **in** range(1, 10\*\*n)]

# 剑指 Offer 18. 删除链表的节点

给定单向链表的头指针和一个要删除的节点的值，定义一个函数删除该节点。返回删除后的链表的头节点。

1. \# Definition for singly-linked list.

2. \# class ListNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.next = None

6. **class** Solution:

7. **def** deleteNode(self, head: ListNode, val: int) -> ListNode:

8. prev = ListNode("dummy")

9. prev.next = head

10. res = prev

11. **while** head:

12. **if** head.val == val:

13. **break**

14. head = head.next

15. prev = prev.next

16. **if** head **is** None:

17. **return** res.next

18. next = head.next

19. prev.next = head.next

20. head.next = None

21. **return** res.next

# 剑指 Offer 19. 正则表达式匹配

请实现一个函数用来匹配包含'. '和'\*'的正则表达式。模式中的字符'.'表示任意一个字符，而'\*'表示它前面的字符可以出现任意次（含0次）。在本题中，匹配是指字符串的所有字符匹配整个模式。例如，字符串"aaa"与模式"a.a"和"ab\*ac\*a"匹配，但与"aa.a"和"ab\*a"均不匹配。

​  

1. **class** Solution:

2. **def** isMatch(self, s: str, p: str) -> bool:

3. m = len(s)

4. n = len(p)

5. dp = [\[False]\*(n+1) **for** \_ **in** range(m+1)\]

6. dp[-1][-1] = True

7.

8. **for** i **in** range(m, -1, -1):

9. **for** j **in** range(n-1, -1, -1):

10. first\_match = i < m **and** (p[j] == "." **or** p[j] == s[i])

11. **if** j + 1 < n **and** p[j+1] == "\*":

12. dp[i][j] = dp[i][j+2] **or** first\_match **and** dp[i+1][j]

13. **else**:

14. dp[i][j] = first\_match **and** dp[i+1][j+1]

15. **return** dp[0][0]

# 剑指 Offer 20. 表示数值的字符串

​  

请实现一个函数用来判断字符串是否表示数值（包括整数和小数）。例如，字符串"+100"、"5e2"、"-123"、"3.1416"、"0123"都表示数值，但"12e"、"1a3.14"、"1.2.3"、"+-5"、"-1E-16"及"12e+5.4"都不是。

​  

1. **class** Solution:

2. **def** isNumber(self, s: str) -> bool:

3. state = [

4. {},

5. \# 状态1,初始状态(扫描通过的空格)

6. {"blank": 1, "sign": 2, "digit": 3, ".": 4},

7. \# 状态2,发现符号位(后面跟数字或者小数点)

8. {"digit": 3, ".": 4},

9. \# 状态3,数字(一直循环到非数字)

10. {"digit": 3, ".": 5, "e": 6, "blank": 9},

11. \# 状态4,小数点(后面只有紧接数字)

12. {"digit": 5},

13. \# 状态5,小数点之后(后面只能为数字,e,或者以空格结束)

14. {"digit": 5, "e": 6, "blank": 9},

15. \# 状态6,发现e(后面只能符号位, 和数字)

16. {"sign": 7, "digit": 8},

17. \# 状态7,e之后(只能为数字)

18. {"digit": 8},

19. \# 状态8,e之后的数字后面(只能为数字或者以空格结束)

20. {"digit": 8, "blank": 9},

21. \# 状态9, 终止状态 (如果发现非空,就失败)

22. {"blank": 9}

23. ]

24. cur\_state = 1

25. **for** c **in** s:

26. **if** c.isdigit():

27. c = "digit"

28. **elif** c **in** " ":

29. c = "blank"

30. **elif** c **in** "+-":

31. c = "sign"

32. **if** c **not** **in** state[cur\_state]:

33. **return** False

34. cur\_state = state[cur\_state][c]

35. **if** cur\_state **not** **in** [3, 5, 8, 9]:

36. **return** False

37. **return** True

# 剑指 Offer 21. 调整数组顺序使奇数位于偶数前面

输入一个整数数组，实现一个函数来调整该数组中数字的顺序，使得所有奇数位于数组的前半部分，所有偶数位于数组的后半部分。

1. **class** Solution:

2. **def** exchange(self, nums: List[int]) -> List[int]:

3. **if** **not** nums:

4. **return** []

5. left = 0

6. right = len(nums) - 1

7.

8. **while** left < right:

9. **while** left < right **and** nums[right]%2 == 0:

10. right -= 1

11. **if** nums[left]%2 == 1:

12. left += 1

13. **else**:

14. nums[left], nums[right] = nums[right], nums[left]

15. left += 1

16. right -= 1

17. **return** nums

# 剑指 Offer 22. 链表中倒数第k个节点

输入一个链表，输出该链表中倒数第k个节点。为了符合大多数人的习惯，本题从1开始计数，即链表的尾节点是倒数第1个节点。例如，一个链表有6个节点，从头节点开始，它们的值依次是1、2、3、4、5、6。这个链表的倒数第3个节点是值为4的节点。

​  

1. \# Definition for singly-linked list.

2. \# class ListNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.next = None

6.

7. **class** Solution:

8. **def** getKthFromEnd(self, head: ListNode, k: int) -> ListNode:

9. **if** k <= 0:

10. **return** head

11. fast = head

12. **while** k > 1 **and** fast:

13. fast = fast.next

14. k -= 1

15. slow = head

16. **while** fast **and** fast.next:

17. fast = fast.next

18. slow = slow.next

19. **return** slow

# 剑指 Offer 24. 反转链表

定义一个函数，输入一个链表的头节点，反转该链表并输出反转后链表的头节点。

​  

1. \# Definition for singly-linked list.

2. \# class ListNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.next = None

6.

7. **class** Solution:

8. **def** reverseList(self, head: ListNode) -> ListNode:

9. cur = head

10. prev = None

11.

12. **while** cur:

13. next = cur.next

14. cur.next = prev

15. prev = cur

16. cur = next

17. **return** prev

# 剑指 Offer 25. 合并两个排序的链表

输入两个递增排序的链表，合并这两个链表并使新链表中的节点仍然是递增排序的。

​  

1. \# Definition for singly-linked list.

2. \# class ListNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.next = None

6.

7. **class** Solution:

8. **def** mergeTwoLists(self, l1: ListNode, l2: ListNode) -> ListNode:

9. **if** **not** l1 **and** **not** l2:

10. **return** None

11. dummy = ListNode("dummy")

12. res = dummy

13. **while** l1 **and** l2:

14. **if** l1.val <= l2.val:

15. dummy.next = l1

16. dummy = l1

17. l1 = l1.next

18. **else**:

19. dummy.next = l2

20. dummy = l2

21. l2 = l2.next

22.

23. dummy.next = l1 **if** l1 **else** l2

24. **return** res.next

# 剑指 Offer 26. 树的子结构

输入两棵二叉树A和B，判断B是不是A的子结构。(约定空树不是任意一个树的子结构)。B是A的子结构，即 A中有出现和B相同的结构和节点值。

​  

​  

1. \# Definition for a binary tree node.

2. \# class TreeNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.left = None

6. \# self.right = None

7.

8. **class** Solution:

9. **def** isSubStructure(self, A: TreeNode, B: TreeNode) -> bool:

10. **if** **not** B:

11. **return** False

12. **if** **not** A **and** B:

13. **return** False

14.

15. **def** back(node1, node2):

16. **if** node1 **is** None **and** node2 **is** None:

17. **return** True

18. **if** (node1 **is** None **and** node2) **or** (node2 **is** None **and** node1):

19. **return** False

20. **if** node1.val != node2.val:

21. **return** False

22. **if** node1.val == node2.val:

23. **if** node2.left **and** node2.right:

24. **return** back(node1.left, node2.left) **and** back(node1.right, node2.right)

25. **elif** node2.left:

26. **return** back(node1.left, node2.left)

27. **else**:

28. **return** back(node1.right, node2.right)

29.

30. stack = [A]

31. **while** stack:

32. \_tmp = []

33. **for** s **in** stack:

34. **if** s.left:

35. \_tmp.append(s.left)

36. **if** s.right:

37. \_tmp.append(s.right)

38. **if** s.val != B.val:

39. **continue**

40. **if** back(s, B):

41. **return** True

42. stack = \_tmp

43. **return** False

# 剑指 Offer 27. 二叉树的镜像

请完成一个函数，输入一个二叉树，该函数输出它的镜像。

​  

1. \# Definition for a binary tree node.

2. \# class TreeNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.left = None

6. \# self.right = None

7.

8. **class** Solution:

9. **def** mirrorTree(self, root: TreeNode) -> TreeNode:

10. **if** **not** root:

11. **return** None

12.

13. left = self.mirrorTree(root.left)

14. right = self.mirrorTree(root.right)

15. root.left = right

16. root.right = left

17. **return** root

​  

# 剑指 Offer 28. 对称的二叉树

请实现一个函数，用来判断一棵二叉树是不是对称的。如果一棵二叉树和它的镜像一样，那么它是对称的。

​  

1. \# Definition for a binary tree node.

2. \# class TreeNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.left = None

6. \# self.right = None

7.

8. **class** Solution:

9. **def** isSymmetric(self, root: TreeNode) -> bool:

10. **if** **not** root:

11. **return** True

12.

13. **def** \_check(root1, root2):

14. **if** **not** root1 **and** **not** root2:

15. **return** True

16. **if** **not** root1 **and** root2:

17. **return** False

18. **if** root1 **and** **not** root2:

19. **return** False

20. **if** root1.val != root2.val:

21. **return** False

22. **return** \_check(root1.left, root2.right) **and** \_check(root1.right, root2.left)

23.

24. **return** \_check(root.left, root.right)

​  

# 剑指 Offer 29. 顺时针打印矩阵

输入一个矩阵，按照从外向里以顺时针的顺序依次打印出每一个数字。

​  

1. **class** Solution:

2. **def** spiralOrder(self, matrix: List[List\[int]\]) -> List[int]:

3. **if** **not** matrix **or** **not** matrix[0]:

4. **return** []

5.

6. m = len(matrix)

7. n = len(matrix[0])

8.

9. begin\_row = 0

10. end\_row = m - 1

11. begin\_col = 0

12. end\_col = n-1

13. res = []

14. **while** True:

15. **for** i **in** range(begin\_col, end\_col+1):

16. res.append(matrix[begin\_row][i])

17. begin\_row += 1

18. **if** begin\_row > end\_row:

19. **break**

20.

21. **for** i **in** range(begin\_row, end\_row+1):

22. res.append(matrix[i][end\_col])

23. end\_col -= 1

24. **if** begin\_col > end\_col:

25. **break**

26.

27. **for** i **in** range(end\_col, begin\_col-1, -1):

28. res.append(matrix[end\_row][i])

29. end\_row -= 1

30. **if** begin\_row > end\_row:

31. **break**

32.

33. **for** i **in** range(end\_row, begin\_row-1, -1):

34. res.append(matrix[i][begin\_col])

35. begin\_col += 1

36. **if** begin\_col > end\_col:

37. **break**

38. **return** res

# 剑指 Offer 30. 包含min函数的栈

定义栈的数据结构，请在该类型中实现一个能够得到栈的最小元素的 min 函数在该栈中，调用 min、push 及 pop 的时间复杂度都是 O(1)。

​  

1. **class** MinStack:

2.

3. **def** \_\_init\_\_(self):

4. """

5. initialize your data structure here.

6. """

7. self.stack1 = []

8. self.min\_num = []

9.

10. **def** push(self, x: int) -> None:

11. self.stack1.append(x)

12. **if** self.min\_num:

13. \_min\_num = self.min\_num[-1]

14. **if** x < \_min\_num:

15. self.min\_num.append(x)

16. **else**:

17. self.min\_num.append(\_min\_num)

18. **else**:

19. self.min\_num.append(x)

20.

21. **def** pop(self) -> None:

22. self.stack1.pop()

23. self.min\_num.pop()

24.

25. **def** top(self) -> int:

26. **if** self.stack1:

27. **return** self.stack1[-1]

28. **else**:

29. **return** None

30.

31. **def** min(self) -> int:

32. **if** self.stack1:

33. **return** self.min\_num[-1]

34. **else**:

35. **return** None

36.

37.\# Your MinStack object will be instantiated and called as such:

38.\# obj = MinStack()

39.\# obj.push(x)

40.\# obj.pop()

41.\# param\_3 = obj.top()

42.\# param\_4 = obj.min()

# 剑指 Offer 31. 栈的压入、弹出序列

输入两个整数序列，第一个序列表示栈的压入顺序，请判断第二个序列是否为该栈的弹出顺序。假设压入栈的所有数字均不相等。例如，序列 {1,2,3,4,5} 是某栈的压栈序列，序列 {4,5,3,2,1} 是该压栈序列对应的一个弹出序列，但 {4,3,5,1,2} 就不可能是该压栈序列的弹出序列。

​  

1. **class** Solution:

2. **def** validateStackSequences(self, pushed: List[int], popped: List[int]) -> bool:

3. **if** **not** pushed **and** **not** popped:

4. **return** True

5. **if** **not** pushed **or** len(pushed) != len(popped):

6. **return** False

7.

8. stack = []

9. point\_popd = 0

10. **for** i **in** range(len(pushed)):

11. stack.append(pushed[i])

12. **while** stack **and** stack[-1] == popped[point\_popd]:

13. stack.pop()

14. point\_popd += 1

15. **if** **not** stack **and** point\_popd == len(popped):

16. **return** True

17. **return** False

# 剑指 Offer 32 - I. 从上到下打印二叉树

从上到下打印出二叉树的每个节点，同一层的节点按照从左到右的顺序打印。

​  

1. \# Definition for a binary tree node.

2. \# class TreeNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.left = None

6. \# self.right = None

7.

8. **class** Solution:

9. **def** levelOrder(self, root: TreeNode) -> List[int]:

10. **if** **not** root:

11. **return** []

12. stack = [root]

13. res = []

14.

15. **while** stack:

16. \_tmp = []

17. **for** s **in** stack:

18. **if** s.left:

19. \_tmp.append(s.left)

20. **if** s.right:

21. \_tmp.append(s.right)

22. res.append(s.val)

23. stack = \_tmp

24. **return** res

# 剑指 Offer 32 - II. 从上到下打印二叉树 II

从上到下按层打印二叉树，同一层的节点按从左到右的顺序打印，每一层打印到一行。

​  

1. \# Definition for a binary tree node.

2. \# class TreeNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.left = None

6. \# self.right = None

7.

8. **class** Solution:

9. **def** levelOrder(self, root: TreeNode) -> List[List\[int]\]:

10. **if** **not** root:

11. **return** []

12. stack = [root]

13. res = []

14.

15. **while** stack:

16. \_tmp = []

17. \_res = []

18. **for** s **in** stack:

19. **if** s.left:

20. \_tmp.append(s.left)

21. **if** s.right:

22. \_tmp.append(s.right)

23. \_res.append(s.val)

24. stack = \_tmp

25. res.append(\_res)

26. **return** res

# 剑指 Offer 32 - III. 从上到下打印二叉树 III

请实现一个函数按照之字形顺序打印二叉树，即第一行按照从左到右的顺序打印，第二层按照从右到左的顺序打印，第三行再按照从左到右的顺序打印，其他行以此类推。

​  

1. \# Definition for a binary tree node.

2. \# class TreeNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.left = None

6. \# self.right = None

7. **from** collections **import** deque

8.

9. **class** Solution:

10. **def** levelOrder(self, root: TreeNode) -> List[List\[int]\]:

11. **if** **not** root:

12. **return** []

13. stack = [root]

14. res = []

15. flags = 1

16.

17. **while** stack:

18. \_tmp = []

19. \_res = deque()

20. **for** s **in** stack:

21. **if** s.left:

22. \_tmp.append(s.left)

23. **if** s.right:

24. \_tmp.append(s.right)

25. **if** flags == 1:

26. \_res.append(s.val)

27. **elif** flags == -1:

28. \_res.appendleft(s.val)

29. stack = \_tmp

30. res.append(list(\_res))

31. flags \*= -1

32. **return** res

# 剑指 Offer 33. 二叉搜索树的后序遍历序列

输入一个整数数组，判断该数组是不是某二叉搜索树的后序遍历结果。如果是则返回 true，否则返回 false。假设输入的数组的任意两个数字都互不相同。

​  

1. **class** Solution:

2. **def** verifyPostorder(self, postorder: List[int]) -> bool:

3. **if** **not** postorder:

4. **return** True

5. root = postorder[-1]

6. index = -1

7. **for** i **in** range(len(postorder)-1):

8. **if** index == -1:

9. **if** postorder[i] > root:

10. index = i

11. **else**:

12. **if** postorder[i] < root:

13. **return** False

14. **return** self.verifyPostorder(postorder[:index]) **and** self.verifyPostorder(postorder[index:-1])

# 剑指 Offer 34. 二叉树中和为某一值的路径

输入一棵二叉树和一个整数，打印出二叉树中节点值的和为输入整数的所有路径。从树的根节点开始往下一直到叶节点所经过的节点形成一条路径。

1. \# Definition for a binary tree node.

2. \# class TreeNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.left = None

6. \# self.right = None

7.

8. **class** Solution:

9. **def** pathSum(self, root: TreeNode, sum: int) -> List[List\[int]\]:

10. **if** **not** root:

11. **return** []

12. state = []

13. res = []

14.

15. **def** back(state, node, sum):

16. **if** node.left **is** None **and** node.right **is** None **and** sum == node.val:

17. res.append(state+[node.val])

18. **return** True

19. sum -= node.val

20. **if** node.left:

21. back(state+[node.val], node.left, sum)

22. **if** node.right:

23. back(state+[node.val], node.right, sum)

24. back(state, root, sum)

25. **return** res

​  

# 剑指 Offer 35. 复杂链表的复制

请实现 copyRandomList 函数，复制一个复杂链表。在复杂链表中，每个节点除了有一个 next 指针指向下一个节点，还有一个 random 指针指向链表中的任意节点或者 null。

​  

1. """

2. \# Definition for a Node.

3. class Node:

4. def \_\_init\_\_(self, x: int, next: 'Node' = None, random: 'Node' = None):

5. self.val = int(x)

6. self.next = next

7. self.random = random

8. """

9. **class** Solution:

10. **def** copyRandomList(self, head: 'Node') -> 'Node':

11. **if** **not** head:

12. **return** head

13.

14. cur = head

15. **while** cur:

16. \_new\_node = Node(cur.val)

17. \_next = cur.next

18. cur.next = \_new\_node

19. \_new\_node.next = \_next

20. \_new\_node.random = cur.random

21. cur = \_new\_node.next

22. cur = head

23. **while** cur:

24. **if** cur.random:

25. cur.next.random = cur.random.next

26. **else**:

27. cur.next.random = cur.random

28. cur = cur.next.next

29.

30. prev = head

31. cur = head.next

32. res = head.next

33. **while** cur:

34. \_next = cur.next

35. **if** \_next:

36. prev.next = \_next

37. cur.next = \_next.next

38. prev = \_next

39. cur = \_next.next

40. **else**:

41. prev = None

42. cur = None

43. **return** res

# 剑指 Offer 36. 二叉搜索树与双向链表

输入一棵二叉搜索树，将该二叉搜索树转换成一个排序的循环双向链表。要求不能创建任何新的节点，只能调整树中节点指针的指向。

1. """

2. \# Definition for a Node.

3. class Node:

4. def \_\_init\_\_(self, val, left=None, right=None):

5. self.val = val

6. self.left = left

7. self.right = right

8. """

9. **class** Solution:

10. **def** treeToDoublyList(self, root: 'Node') -> 'Node':

11. **if** **not** root:

12. **return**

13. stack = [root]

14. cur = root

15. head = None

16. tail = None

17. cur\_node = None

18. prev\_node = None

19.

20. **while** stack:

21. cur = stack.pop()

22. **if** cur:

23. **if** cur.right:

24. stack.append(cur.right)

25. stack.append(cur)

26. stack.append(None)

27. **if** cur.left:

28. stack.append(cur.left)

29. **else**:

30. cur\_node = stack.pop()

31. **if** **not** head:

32. head = cur\_node

33. prev\_node = cur\_node

34. **else**:

35. prev\_node.right = cur\_node

36. cur\_node.left = prev\_node

37. prev\_node = cur\_node

38. prev\_node.right = head

39. head.left = prev\_node

40. **return** head

# 剑指 Offer 37. 序列化二叉树(重要)

请实现两个函数，分别用来序列化和反序列化二叉树。

​  

1. \# Definition for a binary tree node.

2. \# class TreeNode(object):

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.left = None

6. \# self.right = None

7.

8. **class** Codec:

9. **def** \_\_init\_\_(self):

10. self.flag = -1

11.

12. **def** serialize(self, root):

13. """Encodes a tree to a single string.

14.

15. :type root: TreeNode

16. :rtype: str

17. """

18. **if** **not** root:

19. **return** '#'

20.

21. **return** str(root.val)+','+self.serialize(root.left)+','+self.serialize(root.right)

22.

23. **def** deserialize(self, data):

24. """Decodes your encoded data to tree.

25.

26. :type data: str

27. :rtype: TreeNode

28. """

29. self.flag += 1

30. lis = data.split(",")

31.

32. **if** self.flag > len(lis):

33. **return**

34. root = None

35. **if** lis[self.flag] != "#":

36. root = TreeNode(int(lis[self.flag]))

37. root.left = self.deserialize(data)

38. root.right = self.deserialize(data)

39. **return** root

# 剑指 Offer 38. 字符串的排列

输入一个字符串，打印出该字符串中字符的所有排列。你可以以任意顺序返回这个字符串数组，但里面不能有重复元素。

​  

1. **class** Solution:

2. **def** permutation(self, s: str) -> List[str]:

3. **if** **not** s:

4. **return** []

5. state = []

6. res = []

7. s = list(s)

8. hs = set()

9.

10. **def** back(state, s):

11. **if** len(s) == 0:

12. **if** tuple(state) **not** **in** hs:

13. res.append("".join(state))

14. hs.add(tuple(state))

15.

16. **for** i **in** range(len(s)):

17. back(state+[s\[i]\], s[:i] + s[i+1:])

18. back(state, s)

19. **return** res

# 剑指 Offer 39. 数组中出现次数超过一半的数字

数组中有一个数字出现的次数超过数组长度的一半，请找出这个数字。你可以假设数组是非空的，并且给定的数组总是存在多数元素。

​  

1. **class** Solution:

2. **def** majorityElement(self, nums: List[int]) -> int:

3. **if** **not** nums:

4. **return**

5. prev = nums[0]

6. count = 0

7. **for** i **in** range(1, len(nums)):

8. **if** nums[i] == prev:

9. count += 1

10. **else**:

11. **if** count <= 0:

12. prev = nums[i]

13. **else**:

14. count -= 1

15. **return** prev

# 剑指 Offer 40. 最小的k个数（重要）

输入整数数组 arr ，找出其中最小的 k 个数。例如，输入4、5、1、6、2、7、3、8这8个数字，则最小的4个数字是1、2、3、4。

1. **class** Solution:

2. **def** getLeastNumbers(self, tinput: List[int], k: int) -> List[int]:

3. **if** tinput == [] **or** k > len(tinput) **or** k==0:

4. **return** []

5. **def** maxHeap(tinput):

6. **for** i **in** range(len(tinput), -1, -1):

7. **if** 2\*i +1 > len(tinput) **and** 2\*i +2 > len(tinput):

8. **continue**

9. left = 2\*i +1

10. right = 2\*i + 2

11. idx = i

12. **if** left < len(tinput) **and** tinput[left] > tinput[i]:

13. idx = left

14. **if** right < len(tinput) **and** tinput[right] > tinput[i]:

15. idx = right

16. tinput[i], tinput[idx] = tinput[idx], tinput[i]

17. **for** i **in** range(len(tinput)):

18. **if** 2\*i +1 > len(tinput) **and** 2\*i +2 > len(tinput):

19. **continue**

20. left = 2\*i + 1

21. right = 2\*i + 2

22. idx = i

23. **if** left < len(tinput) **and** tinput[left] > tinput[i]:

24. idx = left

25. **if** right < len(tinput) **and** tinput[right] > tinput[i]:

26. idx = right

27. tinput[i], tinput[idx] = tinput[idx], tinput[i]

28. **return** tinput

29. k\_head = [float("inf")]\*k

30. **for** num **in** tinput:

31. **if** num < k\_head[0]:

32. k\_head[0] = num

33. k\_head = maxHeap(k\_head)

34. k\_head.sort()

35. **return** k\_head

# 剑指 Offer 41. 数据流中的中位数（重要）

如何得到一个数据流中的中位数？如果从数据流中读出奇数个数值，那么中位数就是所有数值排序之后位于中间的数值。如果从数据流中读出偶数个数值，那么中位数就是所有数值排序之后中间两个数的平均值。

​  

1. **from** heapq **import** \*

2.

3. ss MedianFinder:

4. **def** \_\_init\_\_(self):

5. self.A = [] \# 小顶堆，保存较大的一半

6. self.B = [] \# 大顶堆，保存较小的一半

7.

8. **def** addNum(self, num: int) -> None:

9. **if** len(self.A) != len(self.B):

10. heappush(self.A, num)

11. heappush(self.B, -heappop(self.A))

12. **else**:

13. heappush(self.B, -num)

14. heappush(self.A, -heappop(self.B))

15.

16. **def** findMedian(self) -> float:

17. **return** self.A[0] **if** len(self.A) != len(self.B) **else** (self.A[0] - self.B[0]) / 2.0

# 剑指 Offer 42. 连续子数组的最大和

输入一个整型数组，数组里有正数也有负数。数组中的一个或连续多个整数组成一个子数组。求所有子数组的和的最大值。要求时间复杂度为O(n)

1. **class** Solution:

2. **def** maxSubArray(self, nums: List[int]) -> int:

3. **if** **not** nums:

4. **return** None

5. res = -float("inf")

6. max\_res = -float("inf")

7.

8. **for** i **in** range(len(nums)):

9. **if** res <= 0:

10. res = max(nums[i], res)

11. **else**:

12. res += nums[i]

13. max\_res = max(max\_res, res)

14. **return** max\_res

# 剑指 Offer 43. 1～n整数中1出现的次数（找规律题，记不住）

输入一个整数 n ，求1～n这n个整数的十进制表示中1出现的次数。例如，输入12，1～12这些整数中包含1 的数字有1、10、11和12，1一共出现了5次。

​  

1. **class** Solution:

2. **def** countDigitOne(self, n: int) -> int:

3. res = 0

4. i = 1

5. **while** i <= n:

6. divider = i \* 10

7. res += (n // divider) \* i + min(max(n % divider - i + 1, 0), i)

8. i \*= 10

9. **return** res

# 剑指 Offer 44. 数字序列中某一位的数字（找规律题，记不住）

数字以0123456789101112131415…的格式序列化到一个字符序列中。在这个序列中，第5位（从下标0开始计数）是5，第13位是1，第19位是4，等等。请写一个函数，求任意第n位对应的数字。

​  

1. **class** Solution:

2. **def** findNthDigit(self, n: int) -> int:

3. digit, start, count = 1, 1, 9

4. **while** n > count:

5. n -= count

6. start \*= 10

7. digit += 1

8. count = 9 \* start \* digit

9. num = start + (n - 1) // digit

10. **return** int(str(num)[(n - 1) % digit])

​  

# 剑指 Offer 45. 把数组排成最小的数

输入一个非负整数数组，把数组里所有数字拼接起来排成一个数，打印能拼接出的所有数字中最小的一个。

思路： mn > nm ，则n 在 m 前

​  

1. **class** Solution:

2. **def** minNumber(self, nums: List[int]) -> str:

3. **if** **not** nums:

4. **return** ""

5. nums = [str(i) **for** i **in** nums]

6. nums = sorted(nums, key=functools.cmp\_to\_key(self.\_sort\_str) )

7. **return** "".join(nums)

8.

9. **def** \_sort\_str(self, num1, num2):

10. **if** int(str(num1) + str(num2)) > int(str(num2) + str(num1)):

11. **return** 1

12. **elif** int(str(num1) + str(num2)) < int(str(num2) + str(num1)):

13. **return** -1

14. **else**:

15. **return** 0

​  

# 剑指 Offer 46. 把数字翻译成字符串

​  

给定一个数字，我们按照如下规则把它翻译为字符串：0 翻译成“a”，1 翻译成“b”，……，11 翻译成“l”，……，25 翻译成“z”。一个数字可能有多个翻译。请编程实现一个函数，用来计算一个数字有多少种不同的翻译方法。

​  

​  

思路：动态规划

​  

1. **class** Solution:

2. **def** translateNum(self, num: int) -> int:

3. **if** num < 0:

4. **return** 0

5. num = str(num)

6. dp = [i **for** i **in** range(len(num)+1)]

7. dp[0] = 1

8. dp[1] = 1

9.

10. **for** i **in** range(2, len(num)+1):

11. dp[i] = dp[i-1]

12. **if** int(num[i-2:i]) >= 10 **and** int(num[i-2:i]) <= 25:

13. dp[i] += dp[i-2]

14. **return** dp[-1]

​  

# 剑指 Offer 47. 礼物的最大价值

在一个 m\*n 的棋盘的每一格都放有一个礼物，每个礼物都有一定的价值（价值大于 0）。你可以从棋盘的左上角开始拿格子里的礼物，并每次向右或者向下移动一格、直到到达棋盘的右下角。给定一个棋盘及其上面的礼物的价值，请计算你最多能拿到多少价值的礼物？

​  

​  

思路：动态规划

​  

1. **class** Solution:

2. **def** maxValue(self, grid: List[List\[int]\]) -> int:

3. **if** **not** grid **or** **not** grid[0]:

4. **return** 0

5. m = len(grid)

6. n = len(grid[0])

7.

8. **for** i **in** range(1, m):

9. grid[i][0] = grid[i][0] + grid[i-1][0]

10. **for** i **in** range(1, n):

11. grid[0][i] = grid[0][i] + grid[0][i-1]

12.

13. **for** i **in** range(1, m):

14. **for** j **in** range(1, n):

15. grid[i][j] = max(grid[i-1][j], grid[i][j-1]) + grid[i][j]

16. **return** grid[m-1][n-1]

# 剑指 Offer 49. 丑数

​  

我们把只包含质因子 2、3 和 5 的数称作丑数（Ugly Number）。求按从小到大的顺序的第 n 个丑数。

​  

​  

思路：三指针指向 2/3/5 对应的位数，每个指针乘以对应数字，取最小的数作为作为新的丑数，并把对应的指针前移前移一步。

​  

1. **class** Solution:

2. **def** nthUglyNumber(self, n: int) -> int:

3. res = [1]

4. two = 0

5. three = 0

6. five = 0

7.

8. **for** i **in** range(1, n):

9. num\_two = res[two] \* 2

10. num\_three = res[three] \* 3

11. num\_five = res[five] \* 5

12.

13. min\_num = min(num\_two, num\_three, num\_five)

14. res.append(min\_num)

15. **if** min\_num == num\_two:

16. two += 1

17. **if** min\_num == num\_three:

18. three += 1

19. **if** min\_num == num\_five:

20. five += 1

21. **return** res[-1]

# 剑指 Offer 50. 第一个只出现一次的字符

​  

在字符串 s 中找出第一个只出现一次的字符。如果没有，返回一个单空格。 s 只包含小写字母。

​  

思路：哈希表

​  

1. **from** collections **import** Counter

2. ss Solution:

3. **def** firstUniqChar(self, s: str) -> str:

4. **if** **not** s:

5. **return** " "

6. counter = Counter(s)

7. **for** i **in** range(len(s)):

8. **if** counter[s\[i]\] == 1:

9. **return** s[i]

10. **return** " "

# 剑指 Offer 51. 数组中的逆序对（不熟，归并排序）

​  

在数组中的两个数字，如果前面一个数字大于后面的数字，则这两个数字组成一个逆序对。输入一个数组，求出这个数组中的逆序对的总数。

​  

​  

思路：用归并排序思想，在进行归并排序时，把逆序对的数计了。

​  

1. **class** Solution:

2. **def** mergeSort(self, nums, tmp, l, r):

3. **if** l >= r:

4. **return** 0

5.

6. mid = (l + r) // 2

7. inv\_count = self.mergeSort(nums, tmp, l, mid) + self.mergeSort(nums, tmp, mid + 1, r)

8. i, j, pos = l, mid + 1, l

9. **while** i <= mid **and** j <= r:

10. **if** nums[i] <= nums[j]:

11. tmp[pos] = nums[i]

12. i += 1

13. inv\_count += (j - (mid + 1))

14. **else**:

15. tmp[pos] = nums[j]

16. j += 1

17. pos += 1

18. **for** k **in** range(i, mid + 1):

19. tmp[pos] = nums[k]

20. inv\_count += (j - (mid + 1))

21. pos += 1

22. **for** k **in** range(j, r + 1):

23. tmp[pos] = nums[k]

24. pos += 1

25. nums[l:r+1] = tmp[l:r+1]

26. **return** inv\_count

27.

28. **def** reversePairs(self, nums: List[int]) -> int:

29. n = len(nums)

30. tmp = [0] \* n

31. **return** self.mergeSort(nums, tmp, 0, n - 1)

​  

# 剑指 Offer 52. 两个链表的第一个公共节点

输入两个链表，找出它们的第一个公共节点。

​  

思路：假设相遇点前A 的距离为 a，B 的距离为b，相遇点到结束距离为 c，则利用 a + c + b = b + c + a，可以让两个指针相遇。

​  

1. \# Definition for singly-linked list.

2. \# class ListNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.next = None

6.

7. **class** Solution:

8. **def** getIntersectionNode(self, headA: ListNode, headB: ListNode) -> ListNode:

9. node1, node2 = headA, headB

10.

11. **while** node1 != node2:

12. node1 = node1.next **if** node1 **else** headB

13. node2 = node2.next **if** node2 **else** headA

14. **return** node1

# 剑指 Offer 53 - I. 在排序数组中查找数字 I

​  

统计一个数字在排序数组中出现的次数

​  

​  

思路：二分查找

​  

1. **class** Solution:

2. **def** search(self, nums: List[int], target: int) -> int:

3. **if** **not** nums:

4. **return** 0

5.

6. res = 0

7. start = 0

8. end = len(nums)

9. **while** start < end:

10. mid = (start+end)//2

11. **if** nums[mid] == target:

12. res += 1

13. \_mid = mid - 1

14. **while** \_mid >= 0 **and** nums[\_mid] == target:

15. res += 1

16. \_mid -= 1

17.

18. \_mid = mid + 1

19. **while** \_mid < len(nums) **and** nums[\_mid] == target:

20. res += 1

21. \_mid += 1

22. **return** res

23. **elif** nums[mid] > target:

24. end = mid

25. **else**:

26. start = mid + 1

27. **return** res

# 剑指 Offer 53 - II. 0～n-1中缺失的数字

一个长度为n-1的递增排序数组中的所有数字都是唯一的，并且每个数字都在范围0～n-1之内。在范围0～n-1内的n个数字中有且只有一个数字不在该数组中，请找出这个数字。

​  

思路：二分查找

​  

1. **class** Solution:

2. **def** missingNumber(self, nums: List[int]) -> int:

3. start = 0

4. end = len(nums)

5. **while** start < end:

6. mid = (start+end)//2

7. **if** nums[mid] != mid:

8. end = mid

9. **else**:

10. start = mid + 1

11. **return** start

# 剑指 Offer 54. 二叉搜索树的第k大节点

​  

给定一棵二叉搜索树，请找出其中第k大的节点。

​  

思路：中序遍历，直接套模板

​  

1. \# Definition for a binary tree node.

2. \# class TreeNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.left = None

6. \# self.right = None

7.

8. **class** Solution:

9. **def** kthLargest(self, root: TreeNode, k: int) -> int:

10. **if** **not** root **or** k <= 0:

11. **return** None

12. stack = [root]

13. cur = root

14.

15. **while** stack:

16. cur = stack.pop()

17. **if** cur:

18. **if** cur.left:

19. stack.append(cur.left)

20. stack.append(cur)

21. stack.append(None)

22. **if** cur.right:

23. stack.append(cur.right)

24. **else**:

25. cur = stack.pop()

26. k -= 1

27. **if** k == 0:

28. **return** cur.val

​  

# 剑指 Offer 55 - I. 二叉树的深度

输入一棵二叉树的根节点，求该树的深度。从根节点到叶节点依次经过的节点（含根、叶节点）形成树的一条路径，最长路径的长度为树的深度。

​  

思路：递归左右子树，选取最大值

​  

1. \# Definition for a binary tree node.

2. \# class TreeNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.left = None

6. \# self.right = None

7.

8. **class** Solution:

9. **def** maxDepth(self, root: TreeNode) -> int:

10. **if** **not** root:

11. **return** 0

12. **return** 1 + max(self.maxDepth(root.left), self.maxDepth(root.right))

# 剑指 Offer 55 - II. 平衡二叉树（有时反映不过来）

输入一棵二叉树的根节点，判断该树是不是平衡二叉树。如果某二叉树中任意节点的左右子树的深度相差不超过1，那么它就是一棵平衡二叉树。

​  

1. \# Definition for a binary tree node.

2. \# class TreeNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.left = None

6. \# self.right = None

7.

8. **class** Solution:

9. **def** isBalanced(self, root: TreeNode) -> bool:

10. **if** **not** root:

11. **return** True

12. **return** self.\_depth(root) != -1

13.

14. **def** \_depth(self, root):

15. **if** **not** root:

16. **return** 0

17. left = self.\_depth(root.left)

18. **if** left == -1:

19. **return** -1

20. right = self.\_depth(root.right)

21. **if** right == -1:

22. **return** -1

23. **return** max(left, right) + 1 **if** abs(left - right) <= 1 **else** -1

# 剑指 Offer 56 - I. 数组中数字出现的次数（不熟）

一个整型数组 nums 里除两个数字之外，其他数字都出现了两次。请写程序找出这两个只出现一次的数字。要求时间复杂度是O(n)，空间复杂度是O(1)。

​  

思路：位运算，根本看不懂好不。。。强行背吧。。。

​  

1. **class** Solution:

2. **def** singleNumbers(self, nums: List[int]) -> List[int]:

3. ret = functools.reduce(**lambda** x, y: x ^ y, nums)

4. div = 1

5. **while** div & ret == 0:

6. div <<= 1

7. a, b = 0, 0

8. **for** n **in** nums:

9. **if** n & div:

10. a ^= n

11. **else**:

12. b ^= n

13. **return** [a, b]

# 剑指 Offer 56 - II. 数组中数字出现的次数 II（位运算，不熟）

在一个数组 nums 中除一个数字只出现一次之外，其他数字都出现了三次。请找出那个只出现一次的数字。

​  

1. **class** Solution:

2. **def** singleNumber(self, nums: List[int]) -> int:

3. ones, twos = 0, 0

4. **for** num **in** nums:

5. ones = ones ^ num & ~twos

6. twos = twos ^ num & ~ones

7. **return** ones

# 剑指 Offer 57. 和为s的两个数字

​  

输入一个递增排序的数组和一个数字s，在数组中查找两个数，使得它们的和正好是s。如果有多对数字的和等于s，则输出任意一对即可。

​  

思路：就是 two sum

​  

1. **class** Solution:

2. **def** twoSum(self, nums: List[int], target: int) -> List[int]:

3. **if** **not** nums:

4. **return** []

5. hash\_map = set([])

6. **for** i **in** range(len(nums)):

7. **if** target - nums[i] **in** hash\_map:

8. **return** [nums\[i], target-nums[i]\]

9. **else**:

10. hash\_map.add(nums[i])

11. **return** []

​  

# 剑指 Offer 57 - II. 和为s的连续正数序列

输入一个正整数 target ，输出所有和为 target 的连续正整数序列（至少含有两个数）。序列内的数字由小到大排列，不同序列按照首个数字从小到大排列。

​  

​  

思路：用滑动窗口法，两个指针指向开始和结束，当和大了就移动左指针，小了就移动右指针，满足条件就添加结果，再继续移动

​  

1. **class** Solution:

2. **def** findContinuousSequence(self, target: int) -> List[List\[int]\]:

3. **if** target <= 2:

4. **return** []

5.

6. left = 1

7. right = 2

8. count = left + right

9. res = []

10. **while** right <= target//2+1:

11. **if** count == target:

12. res.append([i **for** i **in** range(left, right+1)])

13. right += 1

14. count += right

15. **elif** count > target:

16. count -= left

17. left += 1

18. **else**:

19. right += 1

20. count += right

21. **return** res

# 剑指 Offer 58 - I. 翻转单词顺序

输入一个英文句子，翻转句子中单词的顺序，但单词内字符的顺序不变。为简单起见，标点符号和普通字母一样处理。例如输入字符串"I am a student. "，则输出"student. a am I"。

​  

1. **class** Solution:

2. **def** reverseWords(self, s: str) -> str:

3. s = s.strip()

4. **if** **not** s:

5. **return** ""

6. **return** " ".join(s.split()[::-1])

# 剑指 Offer 58 - II. 左旋转字符串

字符串的左旋转操作是把字符串前面的若干个字符转移到字符串的尾部。请定义一个函数实现字符串左旋转操作的功能。比如，输入字符串"abcdefg"和数字2，该函数将返回左旋转两位得到的结果"cdefgab"。

​  

​  

1. **class** Solution:

2. **def** reverseLeftWords(self, s: str, n: int) -> str:

3. **return** s[n:] + s[:n]

# 剑指 Offer 59 - I. 滑动窗口的最大值（不熟）

给定一个数组 nums 和滑动窗口的大小 k，请找出所有滑动窗口里的最大值。

​  

​  

思路：单调队列

​  

1. **class** Solution:

2. **def** maxSlidingWindow(self, nums: List[int], k: int) -> List[int]:

3. n = len(nums)

4. **if** n \* k == 0:

5. **return** []

6. **if** k == 1:

7. **return** nums

8.

9. **def** clean\_deque(i):

10. **if** deq **and** deq[0] == i - k:

11. deq.popleft()

12. **while** deq **and** nums[i] > nums[deq\[-1]\]:

13. deq.pop()

14.

15. deq = deque()

16. max\_idx = 0

17. **for** i **in** range(k):

18. clean\_deque(i)

19. deq.append(i)

20. **if** nums[i] > nums[max\_idx]:

21. max\_idx = i

22. output = [nums\[max\_idx]\]

23.

24. **for** i **in** range(k, n):

25. clean\_deque(i)

26. deq.append(i)

27. output.append(nums[deq\[0]\])

28. **return** output

​  

# 剑指 Offer 59 - II. 队列的最大值（不熟）

请定义一个队列并实现函数 max\_value 得到队列里的最大值，要求函数max\_value、push\_back 和 pop\_front 的均摊时间复杂度都是O(1)。若队列为空，pop\_front 和 max\_value 需要返回 -1

​  

1. **import** queue

2. **class** MaxQueue:

3.

4. **def** \_\_init\_\_(self):

5. self.deque = queue.deque()

6. self.queue = queue.Queue()

7.

8. **def** max\_value(self) -> int:

9. **return** self.deque[0] **if** self.deque **else** -1

10.

11.

12. **def** push\_back(self, value: int) -> None:

13. **while** self.deque **and** self.deque[-1] < value:

14. self.deque.pop()

15. self.deque.append(value)

16. self.queue.put(value)

17.

18. **def** pop\_front(self) -> int:

19. **if** **not** self.deque:

20. **return** -1

21. ans = self.queue.get()

22. **if** ans == self.deque[0]:

23. self.deque.popleft()

24. **return** ans

​  

# 指 Offer 60. n个骰子的点数

把n个骰子扔在地上，所有骰子朝上一面的点数之和为s。输入n，打印出s的所有可能的值出现的概率。你需要用一个浮点数数组返回答案，其中第 i 个元素代表这 n 个骰子所能掷出的点数集合中第 i 小的那个的概率。

​  

思路：动态规划，dp[n][j] = dp[n-1][j-1] + dp[n-1][j-2] + ….+ dp[n-1][1]

​  

1. **class** Solution:

2. **def** twoSum(self, n: int) -> List[float]:

3. dp = [ \[0 **for** \_ **in** range(6\*n+1)] **for** \_ **in** range(n+1)\]

4. **for** i **in** range(1,7):

5. dp[1][i] = 1

6.

7. **for** i **in** range(2,n+1):

8. **for** j **in** range(i,i\*6+1):

9. **for** k **in** range(1,7):

10. **if** j >= k+1:

11. dp[i][j] +=dp[i-1][j-k]

12. res = []

13. **for** i **in** range(n,n\*6+1):

14. res.append(dp[n][i]\*1.0/6\*\*n)

15. **return** res

# 剑指 Offer 61. 扑克牌中的顺子

从扑克牌中随机抽5张牌，判断是不是一个顺子，即这5张牌是不是连续的。2～10为数字本身，A为1，J为11，Q为12，K为13，而大、小王为 0 ，可以看成任意数字。A 不能视为 14。

​  

思路：遍历排，计算不连续空档和 0 的数量，进行比较即可

​  

1. **class** Solution:

2. **def** isStraight(self, nums: List[int]) -> bool:

3. uncontinue = 0

4. count\_zero = 0

5. cur = None

6. nums.sort()

7. **for** i **in** range(len(nums)):

8. **if** nums[i] == 0:

9. count\_zero += 1

10. **else**:

11. **if** cur **is** None:

12. cur = nums[i]

13. **else**:

14. **if** nums[i] == cur:

15. **return** False

16. **if** nums[i] - cur > 1:

17. uncontinue += nums[i] - cur - 1

18. cur = nums[i]

19. **if** count\_zero < uncontinue:

20. **return** False

21. **return** True

# 剑指 Offer 62. 圆圈中最后剩下的数字（找规律，不熟）

0,1,,n-1这n个数字排成一个圆圈，从数字0开始，每次从这个圆圈里删除第m个数字。求出这个圆圈里剩下的最后一个数字。例如，0、1、2、3、4这5个数字组成一个圆圈，从数字0开始每次删除第3个数字，则删除的前4个数字依次是2、0、4、1，因此最后剩下的数字是3。

​  

思路：直接模拟会超时，要找规律

​  

1. **class** Solution:

2. **def** lastRemaining(self, n: int, m: int) -> int:

3. f = 0

4. **for** i **in** range(2, n + 1):

5. f = (m + f) % i

6. **return** f

# 剑指 Offer 63. 股票的最大利润

假设把某股票的价格按照时间先后顺序存储在数组中，请问买卖该股票一次可能获得的最大利润是多少？

​  

思路：找到当前点前的最小值，再用当前值减去最小值则为当前时间的最大收益，遍历可得整体最大收益

​  

1. **class** Solution:

2. **def** maxProfit(self, prices: List[int]) -> int:

3. **if** len(prices) <= 1:

4. **return** 0

5. res = 0

6. minP = float("inf")

7.

8. **for** i **in** range(len(prices)):

9. minP = min(minP, prices[i])

10. res = max(res, prices[i] - minP)

11. **return** res

# 剑指 Offer 64. 求1+2+…+n

求 1+2+...+n ，要求不能使用乘除法、for、while、if、else、switch、case等关键字及条件判断语句（A?B:C）。

思路：运用逻辑短路功能，使得 n 变成递归的终止条件，递归主体是 and 后面的，

1. **class** Solution:

2. **def** sumNums(self, n: int) -> int:

3. **return** n **and** n + self.sumNums(n - 1)

# 剑指 Offer 65. 不用加减乘除做加法

写一个函数，求两个整数之和，要求在函数体内不得使用“+”、“\-”、“\*”、“/”四则运算符号。

​  

1. **class** Solution:

2. **def** add(self, a: int, b: int) -> int:

3. x = 0xffffffff

4. a, b = a & x, b & x

5. **while** b != 0:

6. a, b = (a ^ b), (a & b) << 1 & x

7. **return** a **if** a <= 0x7fffffff **else** ~(a ^ x)

# 剑指 Offer 66. 构建乘积数组

给定一个数组 A[0,1,…,n-1]，请构建一个数组 B[0,1,…,n-1]，其中 B 中的元素 B[i]=A[0]×A[1]×…×A[i-1]×A[i+1]×…×A[n-1]。不能使用除法。

​  

思路：先算下三角，再算上三角。

​  

1. **class** Solution:

2. **def** constructArr(self, a: List[int]) -> List[int]:

3. **if** **not** a:

4. **return** []

5. res = [1]

6. n = len(a)

7. **for** i **in** range(n-1):

8. res.append(res[-1]\*a[i])

9.

10. \_tmp = 1

11. **for** i **in** range(n-1, -1, -1):

12. res[i] = \_tmp \* res[i]

13. \_tmp \*= a[i]

14. **return** res

# 剑指 Offer 67. 把字符串转换成整数

写一个函数 StrToInt，实现把字符串转换成整数这个功能。不能使用 atoi 或者其他类似的库函数。

​  

思路：和第 8 题一样，比较重要

​  

1. INT\_MAX = 2 \*\* 31 - 1

2. INT\_MIN = -2 \*\* 31

3.

4. **class** AC(object):

5. **def** \_\_init\_\_(self):

6. self.sign = 1

7. self.ans = 0

8. self.state = "start"

9. self.table = {"start": ["start", "is\_signed", "in\_number", "end"],

10. "is\_signed": ["end", "end", "in\_number", "end"],

11. "in\_number": ["end", "end", "in\_number", "end"],

12. "end": ["end", "end", "end", "end"]}

13.

14. **def** get\_col(self, c):

15. **if** c.isspace():

16. **return** 0

17. **elif** c.isdigit():

18. **return** 2

19. **elif** c == "+" **or** c == "-":

20. **return** 1

21. **else**:

22. **return** 3

23.

24. **def** get(self, c):

25. self.state = self.table[self.state][self.get\_col(c)]

26. **if** self.state == "in\_number":

27. self.ans = self.ans \* 10 + int(c)

28. self.ans = min(self.ans, INT\_MAX) **if** self.sign == 1 **else** min(self.ans, -INT\_MIN)

29. **elif** self.state == "is\_signed":

30. self.sign = -1 **if** c == "-" **else** 1

31.

32.

33.**class** Solution:

34. **def** strToInt(self, str: str) -> int:

35. ac = AC()

36. **for** s **in** str:

37. ac.get(s)

38. **return** ac.ans \* ac.sign

# 剑指 Offer 68 - I. 二叉搜索树的最近公共祖先（常考）

给定一个二叉搜索树, 找到该树中两个指定节点的最近公共祖先。

​  

思路：利用二叉搜索树的性质进行递归，当 P 和 q 的值在根节点的两侧（包含自身），则返回 root，否则分别对左右子树进行遍历。

​  

1. \# Definition for a binary tree node.

2. \# class TreeNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.left = None

6. \# self.right = None

7.

8. **class** Solution:

9. **def** lowestCommonAncestor(self, root: 'TreeNode', p: 'TreeNode', q: 'TreeNode') -> 'TreeNode':

10. **if** **not** root:

11. **return** None

12. **if** p.val > q.val:

13. tmp = p

14. p = q

15. q = tmp

16. node = root

17. **if** p.val < node.val **and** q.val > node.val:

18. **return** node

19. **elif** p.val < node.val **and** q.val < node.val:

20. **return** self.lowestCommonAncestor(root.left, p, q)

21. **elif** p.val > node.val **and** q.val > node.val:

22. **return** self.lowestCommonAncestor(root.right, p, q)

23. **elif** p.val == node.val:

24. **return** node

25. **elif** q.val == node.val:

26. **return** node

# 剑指 Offer 68 - II. 二叉树的最近公共祖先（不熟）

给定一个二叉树, 找到该树中两个指定节点的最近公共祖先。

​  

思路：相比于上一题，这里变成不同的二叉树了，因此递归时的条件改了

​  

1. \# Definition for a binary tree node.

2. \# class TreeNode:

3. \# def \_\_init\_\_(self, x):

4. \# self.val = x

5. \# self.left = None

6. \# self.right = None

7.

8. **class** Solution:

9. **def** lowestCommonAncestor(self, root: TreeNode, p: TreeNode, q: TreeNode) -> TreeNode:

10. **if** **not** root **or** root == p **or** root == q: **return** root

11. left = self.lowestCommonAncestor(root.left, p, q)

12. right = self.lowestCommonAncestor(root.right, p, q)

13. **if** **not** left **and** **not** right: **return** \# 1.

14. **if** **not** left: **return** right \# 3.

15. **if** **not** right: **return** left \# 4.

16. **return** root \# 2. if left and right:
{% endraw %}
