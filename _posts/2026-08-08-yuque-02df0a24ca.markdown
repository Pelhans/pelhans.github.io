---
title: "1.1_机器学习笔记"
date: 2026-08-08
layout: post
liquid: false
tags: [语雀, NEXT, 大模型]
categories: [NEXT]
yuque: true
---

{% raw %}
1.  [条件随机场 CRF](https://www.yuque.com/yuqueyonghuyit3fz/ni7q4d/tggn73dfq9g8gprp)：（做NER 的可能会问，但手推概率不高，要能说明白模型，知道在LSTM-CRF 中是怎么用的？有什么作用？和HMM 的对比）（手写viterbi， CRF 和LR 的联系，CRF 的损失函数，看博客）

1.  条件随机场的定义，用公式表达是怎么样的？为什么公式长这样？

1.  条件随机场是给定随机变量 X 的条件下，随机变量 Y 的马尔科夫随机场。 条件随机场是判别模型
2.  P(Y|X)。其中X 是团块，最常见的是链式随机场

2.  CRF 的矩阵形式公式是什么？介绍一下它？有什么含义？
3.  给定CRF 模型参数，如何计算给定点的概率和（前后向算法）
4.  CRF 的参数估计：似然函数的推导、参数用什么方法求解、表述参数求解原理和过程（拟牛顿法中的BFGS，牛顿法（记不清了）原理和公式推导，优劣点）
5.  维特比解码介绍，在CRF 中是如何工作的。
6.  手写一下维特比算法？
7.  HMM, CRF, 逻辑回归间的区别与联系（CRF 与LR 间的区别与联系不熟）
8.  LSTM+CRF 里的 CRF 介绍一下，说明工作原理，能用公式表述

2.  隐马尔可夫模型（手推概率不高，能说明白模型，记住基本假设，并知道和CRF 的对比就OK了,[HMM](https://www.yuque.com/yuqueyonghuyit3fz/ni7q4d/iz1e66209w68iohy)）

1.  什么是HMM，用公式表达是怎么样的

1.  HMM是一种生成式模型（generative model），它对联合概率分布 p(X,Y) 进行建模，其中 ( X) 是观测序列，(Y) 是对应的隐状态序列。HMM假设观测是由隐状态生成的，隐状态之间存在转移概率，观测状态依赖于当前的隐状态。

2.  HMM 的基本假设是什么：观测变量条件独立性假设、潜变量的马尔科夫性假设
3.  给定HMM的模型参数λ和一个观察序列 O ，计算出观察序列 O 的概率分布 。HMM 的前向算法介绍一下，后向算法呢？前后向算法呢？用公式表述
4.  HMM 的似然函数是什么？用什么办法学习参数？为什么用EM 算法？
5.  介绍一下用EM 算法
6.  为什么HMM 用EM 求解而CRF 用极大似然估计？

1.  HMM 是基于隐态的，不能直接用极大似然估计（有隐含变量）

7.  推导一下在HMM 中的参数估计流程（BW 算法怎么估计的）
8.  HMM 模型的优缺点，它和CRF 的区别与联系

3.  [SVM](https://www.yuque.com/yuqueyonghuyit3fz/ni7q4d/lop4kz38vsb1gy43)（重点，刚毕业一定要能推的明明白白）

1.  什么是SVM ？SVM是一种二分类模型，它的基本模型是定义在**特征空间上的间隔最大的线性分类器**，SVM的学习策略是间隔最大化，可形式化为一个求解凸二次规划问题。通过核技巧隐式的在输入空间直接求解映射空间中特征向量的内积，使其成为一个非线性分类器。
2.  原始问题是怎么样的？能用公式推导出来么？
3.  什么是支持向量？与超平面最近的点被称为支持向量，也就是使得原始问题约束项成立的点。
4.  为什么要用对偶算法来解决？三个优点，其原因在于**降低了算法的计算复杂度**。在原问题下，算法的复杂度与样本维度相关，即等于权重w的维度，而在对偶问题下，算法复杂度与样本数量有关，即为拉格朗日算子的个数。因此，如果你是做线性分类，且样本维度低于样本数量的话，在原问题下求解就好了，Liblinear之类的线性SVM默认都是这样做的；但如果你是做非线性分类，那就会涉及到升维（比如使用高斯核做**核函数**，其实是将样本升到无穷维），升维后的样本维度往往会远大于样本数量，此时显然在对偶问题下求解会更好。另一方面，我们有分析过，只有在支持向量上的样本对应的拉格朗日算子λ才大于0，其余的λ都是=0，而转为对偶问题的计算对象仅有λ，所以大大降低了计算复杂度。

1.  降低计算复杂度：W模方和维度相关，维度高时吃亏。所有点都用上了，没有用到支持向量的特性。
2.  可以上核函数

5.  转化为对偶问题后，和原问题有什么关系？理论上对偶问题提供了原问题的一个下界.，但当满足KKT 条件和slater 条件时，二者是等价的。
6.  什么是KKT 条件？什么是slater 条件？
7.  推导：将原问题转化为对偶问题
8.  用KKT 条件求解出w 和 b 的推导（对于线性可分向量机和线性支持向量机都要做）
9.  介绍一下核函数？它的本质是什么？**令核函数用 K 表示, 它将原空间中任意两个向量 x,z 映射为特征空间之中对应的向量之间的内积**.。核函数本质不是将特征映射到高维空间，而是找到一种直接在低维空间对高维空间中向量做点积运算的简便方法。
10.  高斯核函数为什么是无穷维的？泰勒展开
11.  线性核、多项式核、高斯核，如何选取合适的核函数？（看维度和样本数量，维度高，样本少用线性核、维度低，样本多用高斯核）
12.  什么是SMO 算法？为什么要用它求 α？初始得两个变量是怎么选的（一个违反约束条件最严重，一个使得α2变化最大）？
13.  推导一下SMO怎么求到解析解的
14.  Hinge loss 描述一下？为什么用它？其他地方什么地方可以用它？
15.  多分类问题中，怎么用SVM：一对一，一对剩余
16.  SVM 与LR 的区别与联系

4.  [逻辑回归](https://www.yuque.com/yuqueyonghuyit3fz/ni7q4d/zlm9tppaqe3ssil9)（重点，必考，要能推倒，如果自己项目中用到了，那就得知道工业落地中的一系列问题）

1.  什么是逻辑回归？简单介绍一下它
2.  它的似然函数能写出来麽？
3.  用梯度下降法，推导出参数更新公式
4.  为什么逻辑回归要用极大似然法来进行参数估计而不是最小二乘法? 逻辑回归的最小二乘法的代价函数不是关于分布参数θ的凸函数(证明凸函数需要证明函数的二阶导数为半负定矩阵)，求解过程中，会得到局部最优，不容易求解全局最优θ
5.  LR 的分类标签是0 和1，现在换成1 和-1，尝试推导损失函数公式和梯度更新公式。
6.  逻辑回归是线性模型还是非线性模型
7.  LR作为一个线性模型，如何拟合非线性情况？将数据或者模型弄到高维空间上
8.  多分类问题怎么适配？
9.  LR可以用核么？（加L2 正则，然后上核）
10.  LR为什么用sigmoid函数。这个函数有什么优点和缺点？为什么不用其他函数？（因为y 服从伯努利分布，通过贝叶斯推导可自然得出sigmoid 分布）
11.  Softmax 损失函数形式是什么？对其参数进行求导。
12.  加LR 工业落地中的问题（项目没用到会问么？）

5.  KNN（我项目中用到过，如果项目中没提到一般不会考）

1.  介绍一下KNN 算法
2.  K 值怎么选（一个超参，可以通过交叉验证等方法来确定，一般来说，k越小，模型越复杂，越大越简单）
3.  距离度量怎么选
4.  多数表决与经验风险最小化的等价性证明
5.  KNN 算法的优缺点
6.  如何解决KNN 算法效率低的缺点？
7.  不平衡样本对 KNN 的影响？对应的解决方式是什么？
8.  KD树了解麽？能介绍一下KD 树麽？它的构建过程是什么样子的？查找过程是什么样子的？

6.  K-means（能说明白和手写伪代码）

1.  什么是K-means，介绍一下算法流程
2.  **手写实现k-means**
3.  k-means和k-NN有什么区别？
4.  K-means 算法的优缺点
5.  EM 算法与 kmeans 的关系（假设z的先验概率为对应类别中心为1，否则0. Xz先验概率为exp(x-u)^2，带入Q函数可以得到）

7.  主成分分析（PCA，项目中没用到一般不会考）

1.  推导一下PCA是怎么来的？
2.  PCA与SVD的区别与联系

1.  两者都是矩阵分解的技术，一个直接分解SVD，一个是对协方差矩阵操作后分解PCA
2.  PCA只与SVD的右奇异向量的压缩效果相同
3.  通过SVD可以得到PCA相同的结果，但是SVD通常比直接使用PCA更稳定。因为在PCA求协方差时很可能会丢失一些精度。例如Lauchli矩阵

3.  介绍一下做PCA 的流程

1.  取 m 个 n 维数据。
2.  将原始数据按列组成 n 行 m 列矩阵 X
3.  将 X 的每一行进行零均值化，即减去这一行的均值
4.  求出协方差矩阵
5.  求出协方差矩阵的特征值及对应的特征向量
6.  将特征向量按对应特征值大小从上到下按行排列成矩阵，取前 k 行组成矩阵 P。

8.  决策树（机器学习岗位会问，对那面很重要，不熟）

1.  介绍一下信息熵、**条件熵**、信息增益、信息增益比的概念
2.  决策树的基本定义
3.  介绍一下ID3（信息增益）
4.  介绍一下C4.5（信息增益比）算法
5.  介绍一下决策树的剪枝，为什么要剪枝，剪枝的损失函数是什么？
6.  什么是CART 树，分类树(Gini(基尼)指数最小化准则和回归树(平方误差最小化准则)的生成准则分别是什么？
7.  基尼系数：基尼系数Gini(D)反映了从数据集D中随机抽取两个样本，其类别标记不一致的概率，因此Gini(D)越小，则数据集D的纯度越高。
8.  CART 回归树的构建流程是什么？
9.  CART 分类树的构建流程是什么？什么是基尼系数？
10.  CART 树的剪枝原理和流程
11.  ID3 算法的优缺点：

1.  ID3不能处理连续值属性
2.  ID3算法不能处理具有缺失值的样本
3.  非常容易过拟合
4.  ID3倾向于选择取值比较多的属性作为决策节点

12.  C4.5 算法优缺点

1.  产生的规则易于理解；准确率较高；实现简单；
2.  可以处理缺失值
3.  对数据进行多次顺序扫描和排序，效率较低
4.  只适合小规模数据集，需要将数据放到内存中

13.  ART 算法的优缺点：

1.  如果样本发生一点点的改动，就会导致树结构的剧烈改变
2.  只用一个特征进行分类决策，还是不够科学

14.  缺失值如何处理？

1.  ID3：不能处理，是指原始的ID3 处理不了，但把C4.5 的处理方法用上去也可以，只是不叫ID3 了。。。
2.  C 4.5：如果是训练数据中出现了缺失数据，那么就会考虑这个缺失数据所有可能的值，按权重分配到子节点。
3.  CART：替代划分法。

15.  ID3、C4.5 和CART 对连续值是如何处理的？

1.  ID3：不能直接处理连续型特征,只能事先将连续型特征转为离散型，才能在ID3中使用
2.  C4.5：采用的相邻数值平均值离散化的方式，这样就会具有多个切分点。具体来说，采用的二分法(bi－partition)机制来处理连续属性。对于连续属性a，首先将n个不同取值进行从小到大排序，选择相邻a属性值的平均值t作为候选划分点，划分点将数据集分为两类，因此有包含n－1个候选划分点的集合，分别计算出每个划分点下的信息增益，选择信息增益最大对应的划分点，仍然以信息增益最大的属性作为分支属性。
3.  CART 直接就可以处理连续值

9.  集成学习（器学习岗位会问，对那面很重要，NLP 的话还是要有了解）

1.  介绍一下Boosting 算法，它的原理是什么？为什么这么做会有效（强弱学习器等价原理）？
2.  常见的boosting 算法有哪些？Adaboost、GBDT、XGBoost、LightGBM
3.  介绍一下Adaboost，它的核心点是什么？（调整数据点的权值分布和计算基本分类器的权重系数. 最后对这些分类器进行集成.）。算法流程是什么？学习器的系数形式是什么？每个点的权重是什么？它们是怎么更新的？最终是怎么集成的？
4.  介绍一下**梯度提升树（GBT），它与传统提升树的区别是什么**？（利用损失函数的负梯度在当前模型的值作为残差的近似值）。为什么要用负梯度来代替残差，有什么好处麽？
5.  介绍一下**GBDT 的算法流程**？（还是Boosting 那一套，先通过计算残差构建数据集，而后拟合残差，这么迭代下去）
6.  如何提高模型的泛化能力？加正则、控制叶节点个数、用部分数据集的不放回采样采样等。
7.  什么是经验风险最小化（像极大似然估计）？什么是结构风险最小化（正则）？
8.  **介绍一下XGBoost？它的GBDT 的区别是什么？****（结构风险最小化，用了Loss的二阶展开）。**
9.  **推导一下XGBoost的损失函数****。**
10.  **XGBoost的算法流程是怎样的，如何优化目标函数？（对现有的叶节点加入一个分裂, 然后考虑分裂之后目标函数降低多少）**
11.  介绍一下Bagging 的算法流程？
12.  Bagging 与Boosting的区别？

1.  Bagging 主要关注降低方差(增强泛化能力), 它能平滑强学习器的方差, 因此它在非剪枝决策树, 神经网络等容易受到样本扰动的学习器上效果更明显.
2.  Boosting 主要关注降低偏差(提升模型拟合能力), 它能将一些弱学习器提升为强学习器. 因此它在 SVM, KNN 等不容易受到样本扰动的学习器上效果更明显.

13.  介绍一下随机森林（RF）（数据随机采样、属性也只是用一部分k = log n、Bagging+CART）
14.  RF 的优点：

1.  训练效率较高: 因为RF 使用的决策树只需要考虑所有属性的一个子集
2.  随机森林简单, 容易实现, 计算开销小
3.  RF 的性能表现很好

15.  RF、GBDT 的区别（重要）

1.  相同点：

1.  都是由多棵树组成，最终的结果都是由多棵树一起决定。

2.  不同点：

1.  集成学习：RF属于bagging思想，而GBDT是boosting思想。
2.  偏差-方差权衡：RF不断的降低模型的方差，而GBDT不断的降低模型的偏差。
3.  训练样本：RF每次迭代的样本是从全部训练集中有放回抽样形成的，而GBDT每次使用全部样本。
4.  并行性：RF的树可以并行生成，而GBDT只能顺序生成(需要等上一棵树完全生成)。
5.  最终结果：RF最终是多棵树进行多数表决（回归问题是取平均），而GBDT是加权融合。
6.  数据敏感性：RF对异常值不敏感，而GBDT对异常值比较敏感。
7.  泛化能力：RF不易过拟合，而GBDT容易过拟合。

16.  GBDT、XGBoost 的区别（重要）

1.  **GBDT是机器学习算法，XGBoost是该算法的工程实现。**
2.  在使用CART作为基分类器时，XGBoost显式地加入了**正则项**来控制模型的复杂度，有利于防止过拟合，从而提高模型的泛化能力。
3.  GBDT在模型训练时只是用了代价函数的一阶导数信息，XGBoost对代价函数进行**二阶泰勒展开**，可以同时使用一阶和二阶导数。（好处：相对于GBDT的一阶泰勒展开，XGBoost采用二阶泰勒展开，可以更为精准的逼近真实的损失函数。需要注意的是，损失函数需要二阶可导。）
4.  传统的GBDT采用CART作为基分类器，XGBoost支持**多种类型的基分类器**，比如线性分类器
5.  传统的GBDT在每轮迭代时使用全部的数据，XGBoost则采用了与随机森林相似的策略，支持对数据进行**列采样**。
6.  传统的GBDT没有涉及对缺失值进行处理，XGBoost能够自动学习出**缺失值的处理策略**。

17.  **XGBoost 的并行是如何做的？**

1.  **XGboost 的模型生成可以拆成3 个步骤**

1.  **计算出了损失函数在当前模型下的二阶导，此为需要拟合的量。**
2.  **根据以上需要拟合的量，开始构建CART树。树的构建，第一步需要选特征，需要计算信息增益比，复杂度之类的。这一步是可以并行的，不同的特征可以互不干扰的算自己的**
3.  **每个特征还需要选最优切分点，(即这个特征的可能值我选哪一个呢？) 这一步又是可以并行互不干扰的计算的**

18.  LR、决策树的对比？

1.  LR是线性模型，可解释性强，很容易并行化，但学习能力有限，**需要大量的人工特征工程**
2.  GBDT是非线性模型，具有天然的特征组合优势，特征表达能力强，但是树与树之间无法并行训练，而且树模型很容易过拟合；
3.  **对于高维稀疏数据，GBDT效果不如LR**。

19.  SVM 和决策树/RF 对比
20.  GBDT的boosting体现在哪里？
21.  解释下随机森林节点的分裂策略，以及它和GBDT做分类有什么区别？哪个效果更好些？为什么？哪个更容易过拟合？为什么？
22.  改变随机森林的训练样本数据量，是否会影响到随机森林学习到的模型的复杂度
23.  信息熵和基尼指数的关系(信息熵在x=1处一阶泰勒展开就是基尼指数)
24.  **XGBoost中如何对树进行剪枝**

1.  **在目标函数中增加了正则项：使用叶子结点的数目和叶子结点权重的L2模的平方，控制树的复杂度。**
2.  **在结点分裂时，定义了一个阈值，如果分裂后目标函数的增益小于该阈值，则不分裂。**
3.  **当引入一次分裂后，重新计算新生成的左、右两个叶子结点的样本权重和。如果任一个叶子结点的样本权重低于某一个阈值（最小样本权重和），也会放弃此次分裂。**
4.  **XGBoost 先从顶到底建立树直到最大深度，再从底到顶反向检查是否有不满足分裂条件的结点，进行剪枝。**

25.  **XGBoost可以做特征选择，它是如何评价特征重要性的？**

1.  **XGBoost中有三个参数可以用于评估特征重要性：**

1.  **weight ：该特征在所有树中被用作分割样本的总次数。**
2.  **gain ：该特征在其出现过的所有树中产生的平均增益。**
3.  **cover ：该特征在其出现过的所有树中的平均覆盖范围。覆盖范围这里指的是一个特征用作分割点后，其影响的样本数量，即有多少样本经过该特征分割到两个子节点。**

26.  **XGBoost怎么处理缺失值？**

1.  **在某列特征上寻找分裂节点时，不会对缺失的样本进行遍历，只会对非缺失样本上的特征值进行遍历，这样减少了为稀疏离散特征寻找分裂节点的时间开销**
2.  **另外，为了保证完备性，对于含有缺失值的样本，会分别把它分配到左叶子节点和右叶子节点，然后再选择分裂后增益最大的那个方向，作为预测时特征值缺失样本的默认分支方向。**
3.  **如果训练集中没有缺失值，但是测试集中有，那么默认将缺失值划分到右叶子节点方向。**

27.  **为什么梯度提升方法倾向于选择决策树（通常是CART树）作为基学习器呢**

1.  **决策树可以认为是if-then规则的集合，易于理解，可解释性强，预测速度快。同时，决策树算法相比于其他的算法需要更少的特征工程，比如可以不用做特征标准化，可以很好的处理字段缺失的数据，也可以不用关心特征间是否相互依赖等。决策树能够自动组合多个特征，它可以毫无压力地处理特征间的交互关系并且是非参数化的，因此你不必担心异常值或者数据是否线性可分**

28.  **树模型怎么处理连续特征**

1.  **若属性a在这n个样本中有m个不同的取值(m<=n)，对这m个值两两之间取中点，可获得m-1个中点作为候选划分点。接下来的选择最佳划分点过程和离散属性的虚选择过程类似，以****基尼系数****或****信息增益****作为度量，选择****使度量值最大****的候选划分点作为最佳划分点。**
2.  **连续特征确实可能导致分割点的数量非常多，尤其是当连续特征的值域很广并且数据点很多时。这种情况下，决策树可能会生成非常复杂的模型，具有许多分割点和深层次的结构。可以进行特征离散化（Feature Binning）： 在训练模型之前，对连续特征进行离散化处理，将其分成若干个区间，以减少潜在的分割点数量。**

29.  XGBoost 为什么要用二阶导

1.  二阶信息本身就能让梯度收敛更快更准确。这一点在优化算法里的牛顿法里已经证实了。可以简单认为一阶导指引梯度方向，二阶导指引梯度方向如何变化（二阶导数是函数图像在某点的曲率）。

30.  随机森林的随机性体现在哪里

1.  数据是随机采样生成的
2.  分裂的属性不是全部属性，是随机选取的 k 个属性

31.  随机森林是不是树越多越好

1.  不是，随机森林中通过引入随机抽样和随机抽列，使模型对异常点有更好的鲁棒性，模型的泛化能力更强。如果是无限颗树，那其实会抵消随机性的引入，最后的模型会是一个过拟合的模型，其泛化性能也会降低。

32.  Stacking 原理
{% endraw %}
