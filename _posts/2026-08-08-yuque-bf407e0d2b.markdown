---
title: "Effective_C++_笔记"
date: 2026-08-08
layout: post
liquid: false
tags: [语雀, 计算机, 编程, 计算机基础]
categories: [计算机]
yuque: true
---

{% raw %}
## 1\. 让自己习惯 C++

### 条款2 使用const、enum、inline定义常量而不用#define

编译过程中，#define 定义的宏会被替换成实际的值（或者函数），导致排查困难，也很容易带来问题，用 const 和 enum 实现常量定义，用 inline 实现函数定义

```cpp
#define C 1

enum {C = 1}; // 第一种
const int C = 1;//第二种

#define CALL_WITH_MAX(a,b) f((a) > (b) ? (a):(b))

template <typename T>
inline void callWithMax(const T& a,const T& b)
{
	f(a > b ? a:b);
}
```

### 条款3 尽可能使用 const

const 允许指定一个语义约束，而编译器会强制实施这项约束，让问题能够提前暴露出来，也要求程序员能够严格的去对成员函数进行区分，另外对于别人使用来说，能够一目了然的知道哪些函数是安全的，哪些函数会修改变量。

-   const 定义变量：const定义变量，表示这个变量不可修改

-   const出现在\*的左边，表示变量指向的值不会改变，如果const在\*的右边，表示该指针是常量

```cpp
const char * s = "hello world";
```

-   const定义函数：const修饰类成员函数，表示这个函数只能被const的对象调用

-   能够提高程序的可读性，哪些函数可写，哪些函数只读一目了然
-   一个例外是 mutable 变量可以修改

```cpp
void getValue() const;
```

-   const引用传递：采用const引用，既可以避免多余的函数调用，也能保证数据不被修改
-   const与线程安全：除了 mutable 变量情况外，当一个函数被声明为const时，就应该保证这个函数是线程安全的

### 条款4 确定对象被使用前已先被初始化

C++ 并不保证变量所有情况下都会被初始化，读取未初始化的值可能会导致不明确的行为。最佳处理办法是：永远在使用对象之前将其初始化，与资源获取即初始化 RAII (Resource Acquisition Is Initialization) 异曲同工

-   内置类型的初始化：为内置类型对象进行手工初始化，因为 C++ 不保证初始化它们

```cpp
int x = 0;    
const char* text = "A C-style string";
```

-   构造函数

-   确保每一个构造函数都将对象的每一个成员初始化。首选使用**成员初值列 (member initialization list)**进行初始化操作，可避免某些不必要的复制操作（原因是会先调用 default 构造函数，如果不用成员初始值列会造成重复赋值）

```cpp
class PhoneNumber {};
class ABEntry {
private:
    std::string theName;
    std::string theAddress;
    std::list<PhoneNumber> thePhones;
    int numTimesConsulted;
public:
    ABEntry(const std::string& name, const std::string& address,
            const std::list<PhoneNumber>& phones)
        : theName{name},           // 调用 theName 的 default 构造函数
          theAddress{address},     // 为 theAddress 做类似的操作
          thePhones{phones},       // 为 thePhones 做类似的操作
          numTimesConsulted(0)     // 记得将 numTimesConsulted 显式初始化为 0
    { }                            // 初始化顺序以声明顺序为准
};
```

-   定义在不同编译单元内的 non-local static 对象

-   C++ 对 定义在不同编译单元内的 non-local static 对象 的初始化次序无明确定义，且无解。为避免定义在不同编译单元内的 non-local static 对象初始化次序问题 ，使用 local static 对象替换non-local static 对象，如 singleton 模式

## 2.构造/析构/赋值 运算

### 条款5 了解 C++默默编写并调用哪些函数

编译器可以暗自为 class 创建 default 构造函数、copy 构造函数、copy assignment操作符，以及析构函数。当你写下类时

```cpp
class Empty {};
```

编译器会为你生成(copy 和 copy assigniment 是根据需求生成的）

```cpp
class Empty {
public:
    Empty() = default;                             // default 构造函数
    ~Empty() = default;                            // default 析构函数
    Empty(Empty&& rhs) = default;                  // default move 构造函数 C++ 1x
    Empty(const Empty& rhs) = default;             // default copy 构造函数
    Empty& operator=(Empty&& rhs) = default;       // default move assigniment 操作符 C++ 1x
    Empty& operator=(const Empty& rhs) = default;  // default copy assigniment 操作符
};
```

### 条款6 若不想使用编译器自动生成的函数，应当明确拒绝

-   为驳回编译器 (暗自) 提供的机能，可将相应的成员函数声明为 private 并且不予实现 (C++ 1x 中通常通过 delete 方式)
-   使用像Uncopyable这样的base class也可以

声明为 private 而且故意不实现它们：

```cpp
// C++ 0x
class HomeForSale {
private:
    HomeForSale(const HomeForSale&);             // 复制构造函数，只有声明
    HomeForSale& operator=(const HomeForSale&);  // 复制赋值运算符
public:
    ...
};
```

delete：

```cpp
// C++ 11 起
class HomeForSale {
public:
    HomeForSale(const HomeForSale&) = delete;             // 弃置复制构造函数
    HomeForSale& operator=(const HomeForSale&) = delete;  // 弃置复制赋值运算符
    ...
};
```

继承自 Uncopyable

```cpp
// C++ 11 起
class Uncopyable {
protected:
    Uncopyable() = default;                             // 子类可调用 构造函数 和 析构函数
    ~Uncopyable() = default;                            // 由于 Uncopyable 不含数据成员，所以不必是 virtual
public:
    Uncopyable(const Uncopyable&) = delete;             // 复制构造函数
    Uncopyable& operator=(const Uncopyable&) = delete;  // 复制赋值运算符
};

class HomeForSale : private Uncopyable {                // private 继承
public:
    ...
};
```

### 条款7 为多态基类声明 virtual 析构函数

-   多态基类 (polymorphic base classes) 应该声明一个 virtual 析构函数，如果 class 带有任何 virtual 函数，它就应该拥有一个 virtual 析构函数
-   class 的设计目的如果不是作为 base classes 使用，或不是为了具备多态性(polymorphically)，就不应噶声明 virtual析构函数（虚表(**vptr (virtual table pointer))**带来内存占用变大等问题）
-   不要企图继承自一些 STL 容器(vector, list, set, unordered\_map, etc.)，因为他们没有声明 virtual 析构函数

```cpp
class TimeKeeper {                              // base class
public:
    TimeKeeper();
    ~TimeKeeper();                              // 非 virtual 析构函数
    ...
};

class AtomicClock : public TimeKeeper { ... };  // drived class

TimeKeeper* ptk = new AtomicClock();
delete ptk;
```

上述代码中，删除 ptk 后会造成一个诡异的局部销毁的对象，这是因为调用的析构函数是 timekeeper 的，从而 atomicclock 的特有部分成员没有被析构掉，导致局部销毁。消除这一问题的做法很简单：给 base class 一个 virtual 析构函数

### 条款8 别让异常逃离析构函数

-   **析构函数** 绝对不要吐出异常。如果一个被析构函数可能抛出异常，**析构函数** 应该捕获它，然后吞下它（不传播）或者结束程序。
-   如果客户需要对某个操作函数运行期间抛出的异常作出反应，那么 `class` 应该提供一个普通函数（而非在析构函数中）执行该操作

  
  

-   析构函数 抛出异常便结束程序

```cpp
DBConn::~DBConn() {
    try { db.close(); }
    catch (...) {
        ...             // 制作 log 日志
        std::absort();  // 终止程序
    }
}
```

-   析构函数吞下异常

```cpp
DBConn::~DBConn() {
    try { db.close(); }
    catch (...) {
        ...  // 制作 log 日志，但不终止程序
    }
}
```

-   更好的策略：重新设计 DBConn 的接口

```cpp
class DBConn {       // DBConnection 管理类
private:
    DBConnection db;
    bool closed;
public:
    ... 
    void close() {
        db.close();
        closed = true;
    }
    ~DBConn() {      // 确保数据库连接总是会被关闭
        if (!closed) {
            try { db.close(); }
            catch (...) {
                ...  // 制作 log 日志
            }
        }
    }
};
```

### 条款9 绝不在构造和析构过程中调用 virtual 函数

-   在构造和析构期间不要调用 virtual 函数，因为这类调用从不下降至 derived class（比起当前执行构造函数和析构函数的那层）

```cpp
class Base{
public:
	explicit Base(){
		op();
	}
	virtual void op() const = 0;
};
class Derived:public Base{
public:
	virtual void op(int a) const{
		cout<<"Hello world"<<endl;
	} 
};

int main(){
	Derived d
	return 0;
}
```

Derived创建之前要先创建Base，也就是说先执行Base的构造函数的时候需要执行 op 函数，但是此时还没有进入Derived，op 函数只是一个 pure 函数，没有实际代码，也就是无法执行。

解决办法是将 op 设计成 non-pure 函数，通过派生类传入定制参数来实现目的

### 条款10 令 operator= 返回一个 reference to\* this

为了让赋值实现连锁形式

```cpp
int x, y, z;
x = y = z = 15;
```

赋值操作符必须返回一个 reference 指向操作符的左侧实参，这是在实现 classes 赋值操作符时应该遵循的协议，除非有一个标新立异的好理由

```cpp
class widget {
public:
    ....
   Widget& operator=(const Widget& rhs) { // 返回类型是个 reference，指向当前对象
       ...
       return *this;  //返回左侧对象
   }
   ....
};
```

### 条款11 在 operator= 中处理 “自我赋值”

自我赋值指的是将自己赋给自己，比如 a[i] = a[j]，当 i==j 时就会出现自我赋值的情况。此时要注意掉进 “在停止使用资源之前意外释放了它” 的陷阱。比如

```cpp
class Bitmap { .... };
class Widget {
    ....
    Widget& operator=(const Widget& rhs) {
        delete pb;
        pb = new Bitmap(*rhs.pb);
        return *this;
    }
private:
    Bitmap* pb
};
```

上述代码会出现 pb 指向已被销毁的 bitmap 对象。一种防御措施是加一个证同测试

```cpp
Widget& operator=(const Widget& rhs) {
        if (this == &rhs) return *this;
        delete pb;
        pb = new Bitmap(*rhs.pb);
        return *this;
}
```

但上面的代码还有问题，这就是当 new 出现异常时，Widget 最终会持有一个指针，指向一块被删除的 Bitmap。如果我们把异常安全性也考虑在内，那么我们就会得到如下方法

```cpp
Widget& operator=(const Widget& rhs) {
        Bitmap* pOrig = pb;      // 记住原先的 pb
        pb = new Bitmap(*rhs.pb);//令 pb 指向*pb 的一个副本
        delete pOrig;            // 删除原先的pb
        return *this;
}
```

另一个使用copy and swap技术的替代方案将在条款29中作出详细解释

### 条款12 复制对象时勿忘其每一个成分

1.  当你给类多加了成员变量时，请不要忘记在拷贝构造函数和赋值操作符中对新加的成员变量进行处理。如果你忘记处理，编译器也不会报错，如下面的代码，你忘记复制 lastTransaction, 编译器也不会报错的

```cpp
void logCall(const std::string& funcName);

class Date {};
class Customer {
public:
    Customer(const Customer& rhs);
    Customer& operator=(const Customer& rhs);
private:
    std::string name;
    Date lastTransaction;
};

Customer::Customer(const Customer& rhs) : name(rhs.name) {
    logCall("Customer copy constructor");
}
Customer& Customer::operator=(const Customer& rhs) {
    logCall("Customer copy assignment operator");
    name = rhs.name;
    return *this;
}
```

2\. 如果你的类有继承，那么在你为子类编写拷贝构造函数时一定要格外小心复制基类的每一个成分，这些成分往往是private的，所以你无法访问它们，你应该让子类使用子类的拷贝构造函数去调用相应基类的拷贝构造函数

```cpp
class PriorityCuttomer: public Customer {
public:
    PriorityCuttomer(const PriorityCuttomer& rhs);
    PriorityCuttomer& operator=(const PriorityCuttomer& rhs);
private:
    int priority;
};

PriorityCuttomer::PriorityCuttomer(const PriorityCuttomer& rhs) : 
    priority(rhs.priority),
    Customer(rhs) // 调用 base class 的 copy 构造函数
{
    logCall("PriorityCuttomer copy constructor");
}

PriorityCuttomer& PriorityCuttomer::operator=(const PriorityCuttomer& rhs) {
    logCall("PriorityCuttomer copy assignment operator");
    Customer::operator=(rhs);  // 对base class 成分进行赋值动作
    priority = rhs.priority;
    return *this;
}
```

除此之外，拷贝构造函数和拷贝赋值操作符，他们两个中任意一个不要去调用另一个，这虽然看上去是一个避免代码重复好方法，但是是荒谬的。其根本原因在于拷贝构造函数在构造一个对象——这个对象在调用之前并不存在；而赋值操作符在改变一个对象——这个对象是已经构造好了的。因此前者调用后者是在给一个还未构造好的对象赋值；而后者调用前者就像是在构造一个已经存在了的对象。不要这么做。应该将共同机能放进第三个函数中，并由两个 coping 函数共同调用。

## 3\. 资源管理

### 条款13 以对象管理资源

-   为防止资源泄露，请使用 RAII 对象，它们在构造函数中获得并在析构函数中释放资源
-   两个常备使用的 RAII classes 分别是 tr1::shared\_ptr 和 auto\_ptr，前者通常是更好的选择，因为起 copy 行为比较直观。而 auto\_ptr 的复制动作会使它（被复制物）指向 null

指望手动释放资源是不靠谱的，这存在该段被跳过的风险，因而导致内存泄漏

```cpp
class Investment {
};

Investment* createInvestment ();
void f() {
    Investment* pInv = createInvestment();
    ...
    delete pInv;
}
```

一个更好的方案是采用 RAII，利用局部对象离开作用域后自动调用析构函数销毁的性质来管理资源。

```cpp
void f() {
    share_ptr<Investment> pInv(createInvestment());
}
```

上面展示了 RAII 的两个关键想法：

-   获得资源后立刻放进管理对象
-   管理对象运用析构函数确保资源被释放

### 条款14 在资源管理类中小心 coping 行为

-   复制 RAII 对象必须一并复制它所管理的资源。所以资源的 copying 行为决定 RAII 对象的copying 行为。
-   普遍而常见的 RAII class copying 行为是：抑制 copying、施行引用计数法（reference counting）。不过其他行为也都可能被实现。

RAII 对象被复制需要特殊考虑（不同 RAII 对象管理同一资源）：

```cpp
void lock(Mutex* pm);          // 锁定pm所指的互斥器
void unlock(Mutex* pm);        // 将互斥器解除锁定

class Lock{
private:
    Mutex* mutexPtr;
public:
    explicit Lock(Mutex* pm) : mutexPtr(pm){
        lock(mutexPtr);                       // 获得资源
    }
    ~Lock(){ unlock(mutexPtr); }              // 释放资源
};

Mutex m;            // 定义需要的互斥器
...
{                   // 建立一个区块用来定义critical section
    Lock ml1(&m);   // 锁定互斥器
    Lock ml2(ml1);  // 复制，会发生什么？
    ...
}                   // 自动接触互斥器锁定
```

-   RAII 对象中禁止复制 -- 见条款6
-   RAII 对象中使用“引用计数法”（shared\_ptr)

```cpp
void lock(Mutex* pm);          // 锁定pm所指的互斥器
void unlock(Mutex* pm);        // 将互斥器解除锁定

class Lock{
private:
    std::shared_ptr<Mutex> mutexPtr;
public:
    explicit Lock(Mutex* pm)      // 以某个Mutex初始化shared_ptr
        : mutexPtr(pm, unlock){   // 以unlock()函数为删除器
        lock(mutexPtr.get());     // 获得资源
    }
    // 默认析构函数会自动调用non-static成员变量的析构函数
    // ~Lock(){ /* mutexPtr.~shared_ptr(); */ }
};
```

### 条款15 在资源管理中提供对原始资源的访问

-   API 往往要求访问原始资源，所以每一个 RAII class 应该提供一个获取资源的方法
-   对原始资源的访问使用显式转换较为安全

```cpp
// auto_ptr和shared_ptr都提供了get成员函数
template<class T>
class shared_ptr{
public:
    typedef T element_type;
private:
    element_type* __ptr_;
public:
    element_type* get() const { return __ptr_; }         // 显式转换
    element_type* operator->() const { return __ptr_; }  // 隐式转换
    element_type& operator*() const { return *__ptr_; }  // 隐式转换
};
```

### 条款16 成对使用 new 和 delete 时要采取相同的形式

-   new 和 delete 搭配使用；new[] 和 delete[] 搭配使用

```cpp
typedef std::string AddressLines[4];
std::string* pal = new AddressLines;

delete pal;     // 错误，未有定义
delete [] pal;  // 正确
```

### 条款17 以独立语句将 newed 对象置入智能指针

newed 直接对象置入智能指针可能造成资源泄露

```cpp
int priority();
void processWidget(shared_ptr<Widget> pw, int priority);

processWidget(new Widget, priority());                      // 错误，shared_ptr构造函数是explicit的
processWidget(shared_ptr<Widget>(new Widget), priority());  // 错误，函数调用顺序不明晰
                                                            // 可能顺序：1、new
                                                            // 2、priority()
                                                            // 3、shared_ptr构造函数
                                                            // 若priority()发生异常
                                                            // new返回的指针将遗失
```

解决方法：分离语句，将对象置入智能指针

```cpp
shared_ptr<Widget> pw(new Widget);
processWidget(pw, priority());
```

## 4\. 设计与声明

### 条款18 让接口容易被正确使用，不易被误用

-   好的接口很容易被正确使用，不容易被误用，你应该在你的所有接口中努力达成这些性质
-   促进正确使用的办法包括接口的一致性，以及与内置类型的行为兼容
-   阻止误用的办法包括建立新类型、限制类型上的操作，束缚对象取值，以及消除客户的资源管理责任

### 条款19 设计 class 犹如设计 type

-   新 type 的对象应该如何被创建和销毁？
-   对象的初始化和对象的赋值应该有什么样的差别？（不要混淆“初始化”和“赋值”）
-   新 type 的对象如果被 pass by value，意味着什么？（copy 构造函数定义）
-   什么是新 type 的“合法值”？（构造函数、赋值操作、setter 函数）
-   新 type 需要配合某个继承图系吗？（virtual 函数、virtual 析构函数）
-   新 type 需要什么样的转换？（条款15：显式转换：explicit 构造，隐式转换：operator Type、->）
-   什么样的操作符和函数对此新 type 而言是合理的？
-   什么样的标准函数应该驳回？（条款06：明确拒绝copy 构造、copy assignment 操作符等）
-   谁该取用新 type 的成员？（访问规则：数据成员的封装，成员函数 API 的开放，是否友元）
-   什么是新 type 的“未声明接口”？
-   你的新 type 有多么一般化？（class template）
-   你真的需要一个新 type 吗？（多个 non-member 函数或 template 是否可以达到目的）

### 条款20 宁以 pass-by-reference-to-const 替换 pass-by-value

-   尽量以 pass-by-reference-to-cosnt 替换 pass-by-value，通常前者比后者高效（没有构造和析构被调用），避免 slicing（对象切割）问题
-   以上规则并不适用于内置类型，以及STL的迭代器和函数对象。对它们而言，pass-by-value 往往比较适当

```cpp
class Window{
public:
    ...
    string name() const;           // 返回窗口名称
    virtual void display() const;  // 显示窗口和内容
};
class WindowWithScrollBars : public Window{
public:
    ...
    virtual void display() const;
};

void printNameAndDisplay(Window w){  //不正确！参数可能被切割。
    cout << w.name();
    w.display();
}

void printNameAndDisplay(const Window& w){
    cout << w.name();
    w.display();
}
```

### 条款21 必须返回对象时，别妄想返回其 reference

-   绝不要返回 pointer 或 reference 指向一个 local stack 对象，或返回一个 reference 指向一个 heap-allocated 对象，或返回 pointer 或 reference 指向一个 local static 对象而有可能同时需要多个这样的对象。一个”必须返回新对象“函数的正确写法是：就让那和函数返回一个新对象。。。

### 条款22 将成员变量声明为 private

-   切记将成员变量声明为 private。这可赋予客户访问数据的一致性、可细微划分访问控制、允诺约束条件获得保证，并提供 class 作者以充分的弹性。考虑一个 public 成员变量变更或消失时对用户代码的影响
-   protected 并不比 public 更具封装性。考虑一个 protected 成员变量变更或消失时对 derived class 代码的影响

### 条款23 宁以 non-member、non-friend 替换 member 函数

-   封装使我们能够改变事物而只影响有限客户（类的内部被外界看到的越少，后续改动影响的就越小）
-   因为member函数和friend函数可以访问class的私有成员，因此破坏了类的封装性；

```cpp
class WebBrowser{
public:
    ...
    void clearCache();
    void clearHistory();
    void removeCookies();
    ...
};

// non-member“便利函数”
void clearBrowser(WebBrowser& wb){
    wb.clearCache();
    wb.clearHistory();
    wb.removeCookies();
}
```

-   可以在该类同一命名空间下，定义相应的功能函数，将该类的对象作为入参，进行对象的操作；而且可以在不同头文件的相同命名空间中定义不同类别的非成员函数，既可以保证类的封装性，包裹弹性，又能保证扩充性

```cpp
// 头文件“webbrowser.h”
namespace WebBrowserStuff{
class WebBrowser{...};
    ...                     // 核心机能，几乎所有客户需要的non-member函数
}

// 头文件“webbrowserbookmarks.h”
namespace WebBrowserStuff{
    ...                     // 与书签相关的便利函数
}

// 头文件“webbrowsercookies.h”
namespace WebBrowserStuff{
    ...                     // 与cookie相关的便利函数
}
```

### 条款24 若所有参数皆需类型转换，请为此采用 non-member 函数（没用过）

-   如果你需要为某个函数的所有参数（包括被this指针所指的那个隐喻参数）进行类型转换，那么这个函数必须是一个non-member函数

```cpp
class Rational{
private:
    ...
public:
    Rational(int numerator = 0,     // non-explicit
             int denominator = 1);  // 允许int-to-Rational隐式转换
    int numerator() const;          // 分子访问函数
    int denominator() const;        // 分母访问函数
    ...
};
const Rantional operator*(const Rational& lhs, const Rational& rhs){
    return Rational(lhs.numerator() * rhs.numerator(),
                    lhs.denominator() * rhs.denominator());
}

Rational result;
Rational oneHalf(1, 2);

result = oneHalf * 2;    // 正确，operator*(oneHalf, 2 /* 隐式转换 */);
result = 2 * oneHalf;    // 正确，operator*(2, oneHalf);
```

### 条款25 考虑写出一个不抛异常的 swap 函数（没用过）

-   当 std::swap 对你的类型效率不高时，提供一个 swap 成员函数，并确定这个函数不抛出异常。
-   如果你提供了一个 member swap，也提供一个 non-member swap 用来调用前者。对于class（而非 template），也请具体化 std::swap。
-   调用 swap 时应对 std::swap 使用 using 声明，然后调用 swap 并且不带任何命名空间修饰。
-   为“用户定义类型”进行 std template 全部具体化是好的，但千万不要尝试在 std 内加入某些 std 而言全新的东西

## 条款 32 - 55 看了没感觉，等再写写代码后补充

## 5\. 实现

### 条款26 尽可能延后变量定义式的出现时间

-   尽可能延后变量定义式的出现。这样做可增加程序的清晰度并改善程序效率
-   尝试延后定义直到能够给它初值实参为止

```cpp
std::string encryptPassword(const std::string& password) {
        // std::string encrypted; // 如果被 if return，   这个定义就浪费了
        if (xxx) {
            return "";
        }
        std::string encrypted(password);
        xxx(encrypted);
        return encrypted;
    }
```

### 条款27 尽量少做转型动作

1.  如果可以，尽量避免转型，特别是在注重效率的代码中避免 dynamic\_cast。如果有个设计需要转型动作，试着发展无需转型的替代设计。
2.  如果转型是必要的，试着将它隐藏于某个函数背后。客户随后可以调用该函数，而不需要将转型放进他们自己的代码内。
3.  宁可使用 C++ 新式转型，不要使用旧式转型。前者很容易辨识出来，而且也比较有着分门别类的作用

详细来看：

两种旧式转型：

-   ( T ) expression
-   T ( expression )

C++ 的四种新式转型

-   const\_cast：通常被用来将对象的常量性移除：const\_cast可以将不可改变值的引用或者不可改变指向内存数据的指针，转换成可以改变值的引用或者可以改变指向内存数据的指针。但前提是：实际的变量的值是可以改变的
-   dynamic\_cast ：类层次结构中安全地进行向上转换。（google 内部约定除单元测试外，不要使用 dynamic\_cast，如果你需要在运行时确定类型信息，说明设计有缺陷）
-   static\_cast：强迫隐式转换。（non-const 转为 const、int 转为 double、pointer-to-base 转为 pointer-to-derived）（常用）
-   reinterpret\_cast：低级转型。（pointer-to-int 转为 int、针对原始内存构建调试用的分配器 allocator 等）

### 条款28 避免返回 handles 指向对象内部成分

-   避免返回 handles（包括 reference、指针、迭代器）指向对象内部。遵守这个条款可增加封装性，帮助 const 成员函数的行为像个 const，并将发生“虚吊”（dangling handles）的可能性降到最低

```cpp
class Point{
private:
    int x,y;
public:
    Point(int _x, int _y) : x(_x),y(_y){}
    void setX(int newval) { x = newval; }
    void setY(int newval) { y = newval; }
    ...
};
struct RectData{
    Point ulhc;
    Point lrhc;
};
class Rectangle{
private:
    shared_ptr<RectData> pData;
    ...
public:
    Rectangle(Point& p1, Point& p2) : pData(new RectData{ p1,p2 }) {}
    Point& upperLeft() const { return pData->ulhc; }
    Point& lowerRight() const { return pData->lrhc; }
    ...
};

Point coord1(0, 0);
Point coord1(100, 100);
const Rectangle rec(coord1, coord2);
// 看这里
rec.upperLeft().SetX(50);  // 自我矛盾，const对象数据指针成员指向值被修改
                           // 破坏了数据的封装性
                           // 正确，const Point& upperLeft() const { ... }
```

### 条款29 为“异常安全”而努力是值得的（理解很浅）

-   异常安全函数即使发生异常也不会泄漏资源（例如异常发生时，仍然能够释放互斥器）或允许任何数据结构败坏（例如用于替换的内存申请失败时，仍然能保持原内存）。这样的函数区分为三种可能的保证：基本型、强烈型、不抛异常型

-   “异常安全”中保证资源绝不泄漏：确保程序发生异常时资源仍然能够得到及时释放， 实现方法参考条款13/14
-   “异常安全”中保证资源绝不损坏：

-   规则：确保程序发生异常时资源仍然能够得到正常访问
-   基本保证——异常发生时，程序内的任何事物仍然保持在有效状态下。状态不确定
-   强烈保证——异常发生时，程序状态不改变
-   不抛异常——承诺绝不抛出异常

-   “强烈保证”往往能够以 copy-and-swap 实现出来，但“强烈保证”并非对所有函数都可实现或具备现实意义。
-   函数提供的“异常安全性”通常最高只等于其所调用之各个函数的“异常安全保证”中的最弱者

### 条款30 透彻了解 inlining 的里里外外

-   将大多数 inline 限制在小型、被频繁调用的函数身上。以便于调试过程和二进制升级更容易，也可使潜在的代码膨胀问题最小化
-   不要只因为 function template 出现在头文件，就将它们声明为 inline

inline 函数（现代 inline 详解：[C++ inline 有什么用？？](https://www.zhihu.com/question/24185638/answer/2404153835)）：

-   优点：可能导致较小的目标码和较高的指令缓存命中率，免除函数调用所带来的额外开销
-   缺点：过度的 inline 函数造成的**代码膨胀**会导致额外的**换页（paging）**行为，**降低指令缓存命中率**（instruction cache hit rate），以及伴随这些而来的**效率损失**。inline 函数的改动会导致使用它的地方**重新编译**，non-inline 仅需要连接即可

inline 申请可能失败的情况：

-   太过复杂的函数（带有循环或递归）
-   virtual 函数（inline 申请一般在编译期完成替换，而调用对应的 virtual 函数一般运行期才知道）
-   函数指针指向的函数；（指针指向时，通常会生成一个 outlined 函数本体）

构造函数和析构函数非常不适合 inline：

-   原因：编译器生成的函数体体积实际可能会非常大

```cpp
class Base{
public
    ...
private:
    string bm1, bm2;
};
class Derived : public Base {s
public:
    Derived() {}
    ...
private:
    string dm1, dm2, dm3;
};

// 编译器为“Derived() {}”产生的简略代码（实际更复杂）
Derived::Derived(){
    Base::Base();                      // 初始化“Base成分”

    try{ dm1.std::string::string(); }  // 试图构造dm1
    catch(...){                        // 如果抛出异常就
        Base::~Base();                 // 销毁base class成分，并
        throw;                         // 传播该异常
    }

    try{ dm2.std::string::string(); }  // 试图构造dm2 
    catch(...){                        // 如果抛出异常就
        dm1.std::string::~string();    // 销毁dm1
        Base::~Base();                 // 销毁base class成分，并
        throw;                         // 传播该异常
    }

    try{ dm3.std::string::string(); }  // 试图构造dm3
    catch(...){                        // 如果抛出异常就
        dm2.std::string::~string();    // 销毁dm2
        dm1.std::string::~string();    // 销毁dm1
        Base::~Base();                 // 销毁base class成分，并
        throw;                         // 传播该异常
    }
}
```

### 条款31 将文件间的编译依存关系降至最低（

-   支持“编译依存最小化”的一般构想是：相依于声明式，不要相依于定义式。基于此构想的两个手段是 handle class 和 interface class
-   程序库头文件应该以“完全且仅有声明式”的形式存在。这种做法不论是否涉及 template 都适用

pimpl（pointer to implementation pointer）：

-   是一种减少代码依赖和编译时间的C++编程技巧，其基本思想是将一个外部可见类(visible class)的实现细节（一般是所有私有的非虚成员）放在一个单独的实现类(implementation class)中，而在可见类中通过一个私有指针来间接访问该实现类  
      
    

```cpp
// 使用Pimpl

// 在头文件person.hpp中
#include <memory>
class Person {
 public:
  Person();
 private:
  // Person类的实现细节放置在该前向声明的实现类中。
  struct Impl;
  // 指向实现类Impl的私有指针
  std::unique_ptr<Impl> pimpl_;
};

// 在源文件person.cpp中
#include "person.hpp"
#include "basic_info.hpp"
#include <string>
#include <memory>
struct Person::Impl {
  std::string name;
  std::string id;
  BasicInfo basic_info;
};
Person::Person() : pimpl_(std::make_unique<Impl>()) {}
```

-   Handle Class：以“声明的依存性”替换“定义的依存性”

-   本质：头文件尽可能自我满足，如果做不到，则让它与其他文件内的声明式（而非定义式）相依
-   原则1：如果使用 object reference 或 pointer 可以完成任务，就不要使用 object
-   原则2：尽量以 class 声明式替换 class 定义式
-   原则3：为声明式（\*\*\*fwd.h，仅包含声明式）和定义式（\*\*\*.h，包含接口或定义）提供不同的头文件
-   缺点：访问的间接性降低了效率；implementation pointer 必须初始化指向一个动态内存，额外的开销以及 bad\_alloc 异常的可能性都会存在

```cpp
// 定义的依存性
#include <string>
#include "date.h"
#include "address.h"
class Person{
public:
    Person(const string& name, const Date& birthDate, const Address& addr);
    string name() const;
    string birthDate() const;
    string address() const;
    ...
private:
    string theName;
    Date theBirthDate;
    Address theAddress;
};

// 声明的依存性
// Person.h
#include <string>    // 标准程序库组件不该被前置声明
#include <memory>

class PersonImpl;    // Person实现类的前置声明           #include "personfwd.h"
class Date;          // Person接口用到的class的前置声明  #include "datefwd.h"
class Address;       // 前置声明                         #include "addressfwd.h"

class Person{
public:
    Person(const string& name, const Date& birthDate, const Address& addr);
    string name() const;
    string birthDate() const;
    string address() const;
    ...
private:
    shared_ptr<PersonImpl> pImpl;
};

// PersonImpl.h，用户不使用该文件创建对象
#include <string>
#include "date.h"
#include "address.h"
class PersonImpl{
public:
    PersonImpl(const string& name, const Date& birthDate, const Address& addr)
        : theName(name), theBirthDate(birthDate), theAddress(addr)
    string name() const { return theName; }
    string birthDate() const { return theBirthDate; }
    string address() const { return theAddress; }
    ...
private:
    string theName;
    Date theBirthDate;
    Address theAddress;
};

// Person.cpp
#include "Person.h"
#include "PersonImpl.h"
Person::Person(const string& name, const Date& birthDate, const Address& addr)
    : pImpl(new PersonImpl(name, birthDate, addr))
{}
string Person::name()const{
    return pImpl->name();
}
...

// 另一个简单的测试
#ifndef ECPP31_H
#define ECPP31_H
class Ecpp28;  // 仅做前置声明

class Ecpp31
{
private:
    //Ecpp28 _28;  // 错误：会分配内存，需要用到完整定义式
    Ecpp28& _ref_28;
    Ecpp28* _p_28;
public:
    Ecpp28 _fun_28(Ecpp28 _28);  // 仅做声明时可以通过编译
    //Ecpp28 _fun_28(Ecpp28 _28) const { return _28; }  错误：// 需要用到完整定义式
};
#endif
```

-   interface class：构造特殊的 abstract base class，详细地描述 derived class 的接口

-   缺点：virtual 函数的调用增加一个间接跳跃的成本；virtual table 增加了对象的内存

```cpp
// Person.h
class Person {
public:
    virtual ~Person();
    virtual string name() const = 0;
    virtual string birthDate() const = 0;
    virtual string address() const = 0;
    static shared_ptr<Person> create(const string& name,
                                     const Date& birthDate,
                                     const Address& addr);
    ...
};

// Person.cpp
shared_ptr<Person> Person::create(const string& name,
                                  const Date& birthDate,
                                  const Address& addr){
    return shared_ptr<Person>(new RealPerson(name, birthDate, addr));
}

// RealPerson.h
class RealPerson :public Person {
private:
    string theName;
    Date theBirthDate;
    Address theAddress;    
public:
    RealPerson(const string& name,
               const Date& birthDate,
               const Address& addr)
    : theName(name), theBirthDate(birthDate), theAddress(addr)
    {}
    virtual ~RealPerson(){}
    string name() const;
    string birthDate() const;
    string address() const;
    shared_ptr<Person> create(const string& name, const Date& birthDate,
        const Address& addr);
    ...
};

// 
string name;
Date dateOfBirth;
Address address;
shared_ptr<Person> pp(Person::create(name, dateOfBirth, address));

pp->name();
```
{% endraw %}
